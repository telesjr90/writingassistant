# Implementation Status

Status source: this file and `docs/roadmap/roadmap_index.yaml` are the current roadmap execution truth layer.

## Active Frontier

- Current track: MVP OMI AI Tool-Assisted Analysis / Candidate Review.
- Owner override decision: `docs/roadmap/decisions/PHASE8-IMPL-023-owner-override-ai-tool-assisted-omi-analysis-required.md` records that deterministic-marker-only OMI extraction is not the MVP target. OMI must analyze raw idea text through an AI/tool-assisted analysis orchestrator that can use Ollama/local models, Story Check, BookNLP, spaCy, NCP, Subtxt, and dramatica-flow, then show evidence-backed candidates for owner review.
- Prior owner override decision: `docs/roadmap/decisions/PHASE8-owner-override-block-full-mvp-closeout-omi-extraction-required.md` remains the full MVP closeout blocker and is refined by the AI/tool-assisted architecture reset.
- Superseded readiness decision: `docs/roadmap/decisions/PHASE8-post-accepted-owner-gate-next-readiness-step-decision.md` is superseded and no longer selects `PHASE8-MVP-COMPLETE-CLOSEOUT-001` as the next step.
- Latest final owner gate decision: `docs/roadmap/decisions/PHASE8-final-owner-accepted-gate-decision.md` remains historically ACCEPTED by explicit owner decision. Owner-gate acceptance remains distinct from full MVP completion and does not retroactively complete MVP.
- Active parent: `PHASE8-IMPL-023 - OMI AI Tool-Assisted Analysis Candidate Review MVP`.
- Active parent records: `docs/roadmap/tasks/PHASE8-IMPL-023.md`, `docs/roadmap/inventory/PHASE8-IMPL-023.md`, `docs/roadmap/enrichment/PHASE8-IMPL-023.enrichment.json`.
- Next child: `PHASE8-IMPL-023-T020F - Manual real owner-authored dramatica-flow-informed rubric validation` (planned). Run one explicit in-process OMI validation with `requested_adapters=["dramatica_flow_informed_rubric"]` and `persist_candidates=False` against controlled owner-authored non-story source. Do not implement T020F here and do not skip to T021.
- Latest child implementation: `PHASE8-IMPL-023-T020E - App-owned dramatica-flow-informed analysis-rubric OMI adapter integration` is complete/PASS. Decision record: `docs/roadmap/decisions/PHASE8-IMPL-023-T020E-app-owned-dramatica-flow-informed-analysis-rubric-omi-adapter-integration.md`. The new explicit-only `dramatica_flow_informed_rubric` identity validates exact T020C requests/results, invokes T020D once, normalizes all four buckets, preserves evidence/references/statement kind/confidence/uncertainty/pending review, converts provenance to the OMI identity, and fails closed without partial findings or persistence. The obsolete T020D frontier assertion was minimally updated without changing evaluator logic. Focused tests: 104 passed; requested regressions: 926 passed. T009/T019E/T020A-T020D remain preserved. T020 remains in progress; T020F is next.
- Latest child implementation: `PHASE8-IMPL-023-T020B - Dramatica-flow integration-path and analysis-only boundary decision` is complete/PASS. Decision record: `docs/roadmap/decisions/PHASE8-IMPL-023-T020B-dramatica-flow-integration-path-and-analysis-only-boundary-decision.md`. Complete runtime is OWNER-BLOCKED / REFERENCE-ONLY. A narrow runtime subset is REJECT/DEFER: `audit` is model-backed and project/truth-state coupled; `status` reports dramatica-flow project state rather than analyzing owner-selected OMI input; `doctor` calls a model/API; remaining CLI paths generate/output prose or mutate state; and no suitable pure/static candidate-only subset was established. A separate app-owned dramatica-flow-informed analysis-rubric path is ACCEPTED for separate implementation under original app-owned rules and labels. T009 identity/schema/support/fixture behavior and T020A hard-false runtime fields remain unchanged. License is unverified despite README MIT claims and no root license file. T020 remains in progress; T020C is next. T020B made docs-only roadmap changes and no code/test/runtime/persistence/prose changes.
- Latest child implementation: `PHASE8-IMPL-023-T020A - Dramatica-flow source/runtime preflight and safety inventory` is complete/PASS. Decision record: `docs/roadmap/decisions/PHASE8-IMPL-023-T020A-dramatica-flow-source-runtime-preflight-and-safety-inventory.md`. T020A adds a focused, read-only dramatica-flow source/runtime preflight in `backend/omi_runtime_preflight.py` and focused tests in `tests/test_omi_live_runtime_preflight_contract.py`. The probe statically inspects only bounded source and editable venv files under `.external_sources/dramatica-flow` and `.external_sources/venvs/dramatica-flow`; it reports package `dramatica-flow` version `0.1.0`, Python `>=3.11`, console script `df = cli.main:app`, editable venv available with exact resolved `file://` source match, runtime surface `installed_reference_surface`, analysis candidate commands `audit` and `status`, prose-production commands `export`, `revise`, `write`, model/network/server evidence, project-mutation evidence, MIT README claim, no root license file, and license not verified. `doctor` is not classified as an authorized analysis command. `dramatica_flow_analysis_only_runtime_authorized`, `dramatica_flow_live_runtime_available`, and `runtime_dependency_available` remain false regardless of source, editable install, command/path config, or enabled flags. T020A does not import or execute dramatica-flow, does not run `df`, does not call subprocess/shell/network/server/model behavior in the probe, does not install anything, and does not mutate `.external_sources/`. No orchestrator, T009 contract, runtime-integration, route, UI, persistence, Memory/Canon, promotion, or prose behavior changed. Focused T020A tests passed: 42 selected tests. The T020 parent is now in progress. Next child is `PHASE8-IMPL-023-T020B - Dramatica-flow integration-path and analysis-only boundary decision`, which must choose between complete runtime owner-blocked/reference-only, a narrowly audited non-generative/non-mutating subset, or an app-owned dramatica-flow-informed analysis rubric. No stage, commit, or push was performed.
- Previous child implementation: `PHASE8-IMPL-023-T019F - Manual real owner-authored Subtxt-informed rubric validation` is complete/PASS. Decision record: `docs/roadmap/decisions/PHASE8-IMPL-023-T019F-manual-real-owner-authored-subtxt-informed-rubric-validation.md`. T019F ran one explicit real in-process OMI orchestrator validation through `analyze_omi_raw_idea_with_tools(requested_adapters=["subtxt_informed_rubric"], persist_candidates=False, project_name="example", source_idea_id="t019f_owner_validation")` against the controlled non-story owner-authored raw idea. The committed T019E built-in `_build_subtxt_informed_rubric_runner` was reached exactly once (`adapter_builder_calls == 1`); the committed T019E built-in runner closure was invoked exactly once (`adapter_runner_calls == 1`); the committed T019D `evaluate_subtxt_informed_semantic_rubric` evaluator was invoked exactly once (`evaluator_calls == 1`). Result-level: `analysis_status: "succeeded"`, `persistence_status: "not_requested"`, `persisted_candidate_ids: []`, `new_candidate_ids: []`, `reused_candidate_ids: []`, `findings: 8` (non-empty). Per-adapter: `adapter_results == [{"adapter": "subtxt_informed_rubric", "state": "succeeded", "explanation": "App-owned Subtxt-informed semantic-rubric adapter produced 8 evidence-backed candidate-only finding(s) through the committed T019D evaluator; OMI adapter identity='subtxt_informed_rubric'; T019C/T019D internal rubric identity='app_owned_subtxt_informed_rubric'; support label='App-owned Subtxt-informed diagnostic support'; status mapping T019C succeeded -> OMI succeeded; no live Subtxt execution; no model call; no Memory/Canon mutation; no promotion/apply-promotion; no story prose.", "candidates": [8 items]}]`; no other adapter ran (`other_adapter_names == []`); the T009 `subtxt` identity was not requested and did not run. Observed category IDs (extracted from the deterministic `raw_finding_id` prefix `rubric_item_NN_<category>`) include all 7 required category IDs: `structural_diagnostic`, `conflict_diagnostic`, `throughline_context_question`, `story_point_context_question`, `source_of_conflict_hypothesis`, `ambiguity`, `owner_review_question` (plus `subject_vs_conflict_question` which is allowed but not required; `insufficient_evidence` correctly decided not to emit because the source has more than 12 word tokens and at least one substantive non-question rule fired). Every finding has `source_adapter == "subtxt_informed_rubric"`, `provenance.adapter == "subtxt_informed_rubric"`, `provenance.tool_source == "subtxt_informed_rubric"`, `provenance.support == "App-owned Subtxt-informed diagnostic support"`, `support_label == "App-owned Subtxt-informed diagnostic support"`, `owner_decision == {"approved": False, "decision": "pending"}`, `review_status == "candidate_review_pending"`, a non-empty `extracted_claim`, a non-empty deterministic `raw_finding_id`, a non-empty `evidence` list, every evidence item carries `source_locator == "source_locator_ref_raw_idea"`, and every `source_excerpt` is an exact substring of the controlled raw idea input. `candidate_type` for every emitted finding belongs to the committed bounded `OMI_SUBTXT_INFORMED_RUBRIC_ALLOWED_FINDING_TYPES` set (`structural_diagnostic`, `conflict_diagnostic`, `diagnostic_question`, `ambiguity`, `evidence_note`); confidence is one of `medium_support` (`structural_diagnostic`, `source_of_conflict_hypothesis`) or `low_support` (questions, `ambiguity`, `conflict_diagnostic`); `high_support` is never emitted per the T019D confidence policy; `uncertainty_label` is one of `null`, `requires_owner_interpretation`, or `ambiguity`. The top-level safety envelope returned by `build_orchestrator_safety_envelope()` is preserved intact with every flag `True` (`no_prose`, `no_memory_canon_mutation`, `no_apply_promotion`, `no_canon_promotion`, `no_real_tool_calls`, `no_story_prose_generation`, `candidate_presence_is_not_canon`, `queue_presence_is_not_approval`, `support_is_not_truth`, `tool_output_is_not_canon`, and the additional `no_package_installs`). Before/after protected snapshots identical: HEAD `5e0d23bd18cefe665ba3bd3e788ee096aa793d4d` stable; `projects/` snapshot byte-for-byte identical (66 files, sorted by relative path, SHA-256 + size + nanosecond mtime); `git status --short -- projects/ .external_sources/` empty before and after. Preserved untracked evidence under `.codex-context/PHASE8-IMPL-023/manual-validation/T019F-subtxt-informed-rubric/` (before-status.txt, after-status.txt, projects-before.json, projects-after.json, protected-paths-before.txt, protected-paths-after.txt, external-sources-before.txt, external-sources-after.txt, raw-orchestrator-result.json, invocation-counts.json, validation-summary.json). Post-T019F automated regressions: `tests/test_omi_subtxt_informed_semantic_rubric_omi_adapter_contract.py` -> `78 passed`; `tests/test_omi_subtxt_informed_semantic_rubric_evaluator.py` -> `81 passed`; `tests/test_omi_subtxt_informed_semantic_rubric_contract.py` -> `91 passed`; `tests/test_omi_tool_assisted_orchestrator_contract.py` -> `30 passed`; `tests/test_omi_tool_assisted_persistence_contract.py` -> `5 passed`; `tests/test_omi_ncp_subtxt_dramatica_flow_adapter_contract.py` -> `59 passed`; `tests/test_omi_live_runtime_preflight_contract.py` -> `89 passed`; combined `433 passed` across 7 contract test files; `python3 -m py_compile backend/omi_analysis_orchestrator.py backend/story_knowledge/subtxt_informed_semantic_rubric_contract.py backend/story_knowledge/subtxt_informed_semantic_rubric_evaluator.py` -> exit 0. The T019 parent (`PHASE8-IMPL-023-T019 - Live Subtxt integration in OMI and analysis`) is now complete/PASS. Live Subtxt runtime remains owner-blocked. The accepted app-owned Subtxt-informed path is now both implemented and manually validated through OMI. The validated app-owned adapter is clearly distinguished from the existing T009 `subtxt` fixture identity. Next task is `PHASE8-IMPL-023-T020 - Live dramatica-flow integration in OMI and analysis` (planned; do not skip to T021, T022, T023, T024, T025, or T026). No stage, commit, or push was performed.
- Previous child implementation: `PHASE8-IMPL-023-T019E - App-owned Subtxt-informed semantic-rubric OMI adapter integration` is complete/PASS. Decision record: `docs/roadmap/decisions/PHASE8-IMPL-023-T019E-app-owned-subtxt-informed-semantic-rubric-omi-adapter-integration.md`. T019E integrates the committed T019D evaluator into the OMI analysis orchestrator under a new app-owned adapter identity `subtxt_informed_rubric`, distinct from the existing T009 `subtxt` adapter identity, distinct from the T019C/T019D internal rubric identity `app_owned_subtxt_informed_rubric`, and distinct from the user-facing support label `App-owned Subtxt-informed diagnostic support`. T019E adds the new identity to `OMI_TOOL_ADAPTER_IDENTITIES` and `OMI_ADAPTER_CONTRACTS`; omits it from `OMI_DEFAULT_ADAPTERS` and `OMI_CONTEXT_ADAPTER_NAMES`; does not route it through the T009 fixture contract; does not support it through `adapter_fixture_outputs`; adds no environment flags; adds no runtime preflight integration; never treats it as live Subtxt; the explicit `requested_adapters=["subtxt_informed_rubric"]` request is the only activation path; the explicit injected `adapter_runners["subtxt_informed_rubric"]` override retains precedence. T019E adds `_build_subtxt_informed_rubric_request`, `_build_subtxt_informed_rubric_runner`, and `_normalize_subtxt_informed_rubric_item` helpers. The T019C request is constructed from the OMI `project_name` and `raw_idea` with safe `project_name` preservation (or a deterministic safe `project_<16 hex>` alias when the `project_name` is unsafe) and all 13 T019C safety confirmations set to `True`. The built-in runner is in-memory only, validates `adapter_config` as `dict|None`, lazily imports the T019C validator/constants and the T019D evaluator, never reads files / env vars / project storage / external documentation, never calls a model, never persists anything, and invokes the T019D evaluator exactly once per runner call. The T019C status maps to OMI `succeeded` / `empty` / `failed_closed` / `error` with non-empty explanations and empty `candidates` on every non-`succeeded` state; malformed/unsafe/exception evaluator results fail closed; one invalid normalized item fails the whole adapter call closed (no partial candidate set is ever returned). T019C `candidate_support` and `diagnostic_questions` are combined and sorted by `item_id` for deterministic ordering. OMI provenance uses the OMI adapter identity `subtxt_informed_rubric` (not the T019C internal identity) because `validate_normalized_finding` requires `provenance.adapter` / `provenance.tool_source` / `source_adapter` to match. T019C internal provenance is validated before conversion and is not duplicated. Evidence excerpts and source locators are preserved unchanged; `extracted_claim` equals the per-category app-owned `diagnostic_text` (never derived from owner source); `owner_decision` stays `{"approved": False, "decision": "pending"}` and `review_status` stays `candidate_review_pending` on every item. `persist_candidates=False` produces `persistence_status="not_requested"` and empty `persisted_candidate_ids` / `new_candidate_ids` / `reused_candidate_ids`. T009 `subtxt`, T009 `OMI_SUBTXT_SCHEMA_VERSION`, T009 `Subtxt diagnostic support only`, T019A `subtxt_live_runtime_available: False`, `analysis_runtime_integration`, T013 runtime preflight, T014C / T015C / T016C / T017B / T018B live adapter branches, `deterministic_fallback`, and persistence are all preserved unchanged. No contract/evaluator/preflight/main/project_manager/other backend module was modified. No existing test file was modified except for the minimum one-line addition of `subtxt_informed_rubric` to the existing orchestrator test file's `REQUIRED_ADAPTERS` allowlist set (required by the explicit-only adapter gate that mandates adding the identity to `OMI_TOOL_ADAPTER_IDENTITIES`). 78 focused in-memory tests in `tests/test_omi_subtxt_informed_semantic_rubric_omi_adapter_contract.py` pass. Combined T019E + T019D + T019C + existing-regression validation: T019E `78 passed`; T019D `81 passed`; T019C `91 passed`; T009 `59 passed`; orchestrator `30 passed`; persistence `5 passed`; `analysis_runtime_integration` contract `103 passed`; handoff `26 passed`; safety regression `115 passed`; preflight `89 passed`. Live Subtxt runtime remains owner-blocked. The T019E app-owned adapter integration is implemented and was not yet manually validated at the time of T019E closeout; `PHASE8-IMPL-023-T019F - Manual real owner-authored Subtxt-informed rubric validation` (planned) was the recommended next task.
- Previous child implementation: `PHASE8-IMPL-023-T019C - App-owned Subtxt-informed semantic-rubric contract` is complete/PASS. Decision record: `docs/roadmap/decisions/PHASE8-IMPL-023-T019C-app-owned-subtxt-informed-semantic-rubric-contract.md`. T019C implements a pure, app-owned input/output contract for future Subtxt-informed semantic-rubric evaluation: a new `app_owned_subtxt_informed_rubric` identity distinct from the T009 `subtxt` adapter identity, a new `App-owned Subtxt-informed diagnostic support` user-facing label distinct from the T009 `Subtxt diagnostic support only` label, and three public validator/safe-builder APIs (`validate_subtxt_informed_rubric_request`, `validate_subtxt_informed_rubric_result`, `build_subtxt_informed_rubric_fail_closed_result`). The contract is implemented at `backend/story_knowledge/subtxt_informed_semantic_rubric_contract.py` and is standard-library only (`copy`, `re`, `typing`), in-memory only, fail-closed, with no file I/O, no env-var reads, no subprocess/shell/network/model/runtime operations, no `.external_sources` import or read, no copied Subtxt documentation, no evaluator, no OMI adapter, no route, no UI, no candidate persistence, no review-queue creation, no Memory/Canon mutation, no promotion record, no apply-promotion, and no story prose. 91 focused in-memory tests in `tests/test_omi_subtxt_informed_semantic_rubric_contract.py` pass. T009 contract (`OMI_SUBTXT_SCHEMA_VERSION = "omi_subtxt_diagnostic_handoff.v1"`, T009 adapter identity `subtxt`, T009 support label `Subtxt diagnostic support only`, T019A `subtxt_live_runtime_available: False`, `analysis_runtime_integration` Subtxt boundary) is preserved unchanged. The normalized valid result is compatible with the existing generic `analysis_runtime_integration.validate_analysis_runtime_output` when validated against an allowlist record whose `output_classes_allowed` includes `rubric_mapping_support`; the generic validator is not modified. Combined regression: `tests/test_omi_subtxt_informed_semantic_rubric_contract.py` -> `91 passed`; T005/T009/T010/T011/T013/T018/T019A/T019B regression suites all still pass. Live Subtxt runtime remains OWNER-BLOCKED. The T019C contract is complete but no evaluator, adapter, route, UI, or OMI integration exists yet. T019D (App-owned Subtxt-informed semantic-rubric evaluator) is the recommended next task.

- Previous child implementation: `PHASE8-IMPL-023-T018C-rerun - Manual real NCP validation rerun after T018C1 allowlist repair` is complete/PASS. Decision record: `docs/roadmap/decisions/PHASE8-IMPL-023-T018C-manual-real-ncp-validation.md`. T018C-rerun ran the committed T018B live NCP candidate-import validation adapter, behind the T018C1-repaired `_ncp_resolve_allowed_input_path` allowlist, through `analyze_omi_raw_idea_with_tools` against exactly one explicit real owner-selected NCP JSON file: `/home/tjrpirateking/projects/WritingAssistantApplication/.external_sources/narrative-context-protocol/examples/complete-space-adventure-storyform.json` (NCP `schema_version: 1.2.0`, 131824 bytes, regular file, not a symlink, not a directory, SHA-256 `34689dab914948b0461952ce1d3e77a7910822f392556510a5b28c750a32543d`; the canonical rich reference fixture documented in the NCP source repo's `VALIDATION.md`; the same selected file T018C used; the T018C1 commit did not modify the file). Runtime flags: `OMI_LIVE_TOOLS_ENABLED=1 OMI_LIVE_NCP_ENABLED=1 OMI_LIVE_NCP_INPUT_PATH=<selected>`; `OMI_LIVE_NCP_BLOCKED` unset; `OMI_LIVE_NCP_VALIDATE_WITH_NODE` unset (default Node-free path; the optional `npm run validate:file -- <selected>` that T018C ran is intentionally not re-run in T018C-rerun because the selected file is unchanged and the T018C-rerun task explicitly disallows re-running it; the original T018C `npm run validate:file: PASS` evidence is preserved in `.codex-context/PHASE8-IMPL-023/manual-validation/T018C-ncp/optional-npm-validate-file-result.txt` and in `docs/roadmap/decisions/PHASE8-IMPL-023-T018C-manual-real-ncp-validation.md`); orchestrator called with `project_name="example"`, `raw_idea="Owner-selected NCP candidate-import validation rerun after T018C1."`, `requested_adapters=["ncp"]`, `persist_candidates=False`. The live NCP adapter was reached through the OMI orchestrator exactly once, accepted the allowlisted `.external_sources/...` path whose absolute `Path.parts` include the literal `projects` ancestor directory outside the repo (the central T018C bug that T018C1 fixed), read the JSON, parsed it, walked the subtext/storytelling containers, and emitted a non-empty candidate-only envelope through the T009 `omi_ncp_context_handoff.v1` envelope shape. Result-level: `analysis_status: "succeeded"`, `persistence_status: "not_requested"`, `persisted_candidate_ids: []`, `new_candidate_ids: []`, `reused_candidate_ids: []`, `findings: 64`, all of `candidate_type: "story_fact"` (one per NCP subtext `storypoint` / `storybeat` item, mapped by the T018B runner through the T009 envelope; the T018B mapping is the contract per T018B and is intentionally limited to the T018B-supported subset of the NCP subtext containers; the live NCP fixture happened to only emit `story_fact` for the current container-walk). Per-adapter: `adapter_results: [{"adapter": "ncp", "state": "succeeded", "explanation": "Live NCP candidate-import adapter read the owner-selected NCP JSON file ... and converted 64 evidence-backed item(s) into candidate-only NCP findings through the existing T009 fixture envelope shape. NCP output is schema/interchange support only; all findings are pending owner decision and must be reviewed before any use.", "candidates": [64 items]}]`. Result-level safety envelope preserved intact (`no_prose`, `no_memory_canon_mutation`, `no_apply_promotion`, `no_canon_promotion`, `no_real_tool_calls`, `no_package_installs`, `no_story_prose_generation`, `candidate_presence_is_not_canon`, `queue_presence_is_not_approval`, `support_is_not_truth`, `tool_output_is_not_canon` all `True`). Every finding has the required evidence/provenance/source-locator / pending-owner / candidate-review-pending fields (verified directly against `raw-orchestrator-result.json`: non-empty `source_excerpt`, non-empty RFC 6901 `source_locator` JSON pointer, `provenance.tool_source == "ncp"`, `owner_decision == {"approved": false, "decision": "pending"}`, `review_status == "candidate_review_pending"`, `source_adapter == "ncp"`, `support_label == "NCP context support only"`). Side-effect checks clean: `candidate_paths_changed: false`, `promotion_paths_changed: false`, `projects/example/**` SHA-256 + size + mtime snapshot identical before/after (`cmp -s project-files-before.sha256 project-files-after.sha256` returned `0`; `cmp -s project-files-before.metadata project-files-after.metadata` returned `0`); `.external_sources/narrative-context-protocol/**` status identical before/after (`cmp -s external-source-status-before.txt external-source-status-after.txt` returned `0`); `git status --short --branch` after the run shows only the pre-existing unrelated leftovers; `git diff --check` clean. The original T018C `PASS-SAFE-FAIL-CLOSED` history is preserved unchanged; T018C-rerun does not erase the T018C failure record, it adds a clearly labeled rerun-after-T018C1 section. The post-T018C1 automated regression suite still passes: `tests/test_omi_ncp_subtxt_dramatica_flow_adapter_contract.py -> 59 passed`; `tests/test_omi_tool_assisted_orchestrator_contract.py -> 30 passed`; `tests/test_omi_tool_assisted_persistence_contract.py -> 5 passed`; `tests/test_omi_live_runtime_preflight_contract.py -> 72 passed`; `python3 -m py_compile backend/omi_analysis_orchestrator.py -> exit 0`; `python3 -m json.tool docs/roadmap/enrichment/PHASE8-IMPL-023.enrichment.json >/dev/null -> exit 0`; `python3 scripts/check_enrichment.py -> exit 0`; `python3 scripts/validate_roadmap.py -> exit 0`. The compact-summary helper-script `all_findings_have_required_evidence: false` is a known helper-script bug (the helper script asserts `owner_decision == "pending"` (string), but the real field is `owner_decision == {"approved": false, "decision": "pending"}` (dict)); the raw-result direct check confirms every finding has the required fields. The helper-script bug does not affect the live adapter, the result-level safety envelope, the side-effect / git status evidence, or the T018C-rerun PASS classification. The live `analyze_omi_raw_idea_with_tools` orchestrator call was invoked exactly once; no retry was required. T018C-rerun did not edit `backend/omi_analysis_orchestrator.py`, `backend/omi_runtime_preflight.py`, any tests, any frontend files, any package/dependency files, any project data under `projects/`, any candidate queue/storage files, any promotion files, `.external_sources/`, `ai_context/`, `graphify-out/`, any `.codex-context/` path except the exact evidence directory above, source caches, generated context packs, runtime source copies, training files, models, datasets, or `artifacts/mvp-readiness/owner-acceptance/`; did not run `npm install`, `npm audit fix`, start a Node server, run `npm run validate:file` over the selected file (intentionally not re-run; T018C evidence preserved), generate story prose, mutate Memory/Canon, create promotion records, run apply-promotion, or stage/commit/push. Untracked evidence under `.codex-context/PHASE8-IMPL-023/manual-validation/T018C-rerun-ncp/` (intentionally not staged, not committed, not pushed). The T018 sequence (`T018A` schema-validator preflight, `T018B` candidate-import validation adapter, `T018C` manual real NCP validation, `T018C1` allowlist repair, `T018C-rerun` validation rerun) closes to PASS. The next parent-level task is `PHASE8-IMPL-023-T019A — Subtxt docs/source preflight`. The T018A npm audit caveat (`ajv` moderate, `fast-uri` high) remains a recorded, owner-known evidence note.

- Previous child implementation: `PHASE8-IMPL-023-T018C1 - Repair NCP input-path allowlist overreach` is complete/PASS. Decision record: `docs/roadmap/decisions/PHASE8-IMPL-023-T018C1-ncp-input-path-allowlist-repair.md`. T018C1 is a narrow backend repair + regression tests + docs/status task that replaces the T018B/T018C `_ncp_resolve_allowed_input_path` broad segments-walk (`for part in resolved.parts: if part == "projects": return None` plus the equivalent broad segments walks for `artifacts` / `graphify-out` / `ai_context` / `.codex-context`) with a strict resolved-root containment check (`_ncp_path_is_relative_to`, using `Path.is_relative_to` when available and otherwise `relative_to` + `ValueError`) against the resolved `(repo_root / "projects")` / `(repo_root / "artifacts")` / `(repo_root / "graphify-out")` / `(repo_root / "ai_context")` / `(repo_root / ".codex-context")` roots in `backend/omi_analysis_orchestrator.py`. A safe `.external_sources/narrative-context-protocol/examples/...` NCP file is now accepted even when the absolute `Path.parts` include an ancestor directory outside the repo named `projects` (the central T018C bug). A file actually inside the resolved `repo_root / "projects"` (or any other forbidden repo-local tree) is still rejected. The T018B traversal-segment / symlink (pre- and post-`Path.resolve`) / directory / not-a-file / `tests_root` / `temp_root` / `external_root` allowlist contract is preserved. T018C1 added 10 new T018C1 regression tests + 1 minor rewrite of the T018B `test_live_ncp_path_allowlist_rejects_projects_tree` test to align with the T018C1 contract in `tests/test_omi_ncp_subtxt_dramatica_flow_adapter_contract.py`. All T018B / T009 / orchestrator / persistence / preflight tests still pass. NCP remains a schema/interchange validation surface only, not an automatic analysis runtime, not canon/truth. No `npm install`, no `npm audit fix`, no Node server, no `npm run validate:file` over project data, no Memory/Canon mutation, no automatic promotion record, no automatic apply-promotion, no NCP candidate persistence, and no story prose generation occurred during T018C1. T018C1 is narrowly scoped and does NOT run the full manual real NCP validation as completion proof — that belongs to the T018C-rerun after T018C1, which is now complete/PASS.

- Previous child implementation: `PHASE8-IMPL-023-T018C - Manual real NCP validation` is complete/PASS-SAFE-FAIL-CLOSED. Decision record: `docs/roadmap/decisions/PHASE8-IMPL-023-T018C-manual-real-ncp-validation.md`. T018C ran the committed T018B live NCP candidate-import validation adapter through `analyze_omi_raw_idea_with_tools` against exactly one explicit real owner-selected NCP JSON file: `/home/tjrpirateking/projects/WritingAssistantApplication/.external_sources/narrative-context-protocol/examples/complete-space-adventure-storyform.json` (NCP `schema_version: 1.2.0`, 131824 bytes, regular file, not a symlink, not a directory; the canonical rich reference fixture documented in the NCP source repo's `VALIDATION.md`). Optional single-file `npm run validate:file -- <selected>` against the pre-existing `node_modules` returned `PASS` (no `npm install`, no `npm audit fix`, no Node server, no project-data validation). Runtime flags: `OMI_LIVE_TOOLS_ENABLED=1 OMI_LIVE_NCP_ENABLED=1 OMI_LIVE_NCP_INPUT_PATH=<selected>`; `OMI_LIVE_NCP_BLOCKED` unset; `OMI_LIVE_NCP_VALIDATE_WITH_NODE` unset (default Node-free path); orchestrator called with `project_name="example"`, `raw_idea="Owner-selected NCP candidate-import validation run."`, `requested_adapters=["ncp"]`, `persist_candidates=False`. Result-level: `analysis_status: "fail_closed"`, `persistence_status: "not_requested"`, `persisted_candidate_ids: []`, `new_candidate_ids: []`, `reused_candidate_ids: []`, `finding_count: 0`, `finding_types: []`. Per-adapter: `adapter_results: [{"adapter": "ncp", "state": "unavailable", "explanation": "Live NCP runner received an unsafe or unresolvable OMI_LIVE_NCP_INPUT_PATH: '...'. The live NCP adapter only accepts explicit owner-selected paths inside the allowlisted test/temp/.external_sources trees and refuses project data, traversal, symlinks, directories, and hidden unsafe locations. Failing closed with no candidates.", "candidates": []}]`. Result safety envelope preserved intact (`no_prose`, `no_memory_canon_mutation`, `no_apply_promotion`, `no_canon_promotion`, `no_real_tool_calls`, `no_package_installs`, `no_story_prose_generation`, `candidate_presence_is_not_canon`, `queue_presence_is_not_approval`, `support_is_not_truth`, `tool_output_is_not_canon` all `True`). Side-effect checks clean: `candidate_paths_changed: false`, `promotion_paths_changed: false`, `project_files_changed: false`, `project_files_added: []`, `project_files_removed: []`, `.external_sources/**` unmodified, `git status --short --branch` shows only the pre-existing unrelated leftovers, `git diff --check` clean. The selected NCP file is otherwise valid (`npm run validate:file` returns `PASS`), but the T018B `_ncp_resolve_allowed_input_path` overreaching path-parts walk at `backend/omi_analysis_orchestrator.py:4650-4663` rejects any path with a literal `projects` segment anywhere, which incorrectly rejects the selected file because this workspace parent directory is literally named `projects` (`/home/tjrpirateking/projects/WritingAssistantApplication/`). The fail-closed path is correct and safe; the bug is in the allowlist granularity, not in the safety contract. T018C is therefore PASS-SAFE-FAIL-CLOSED (live NCP adapter reached through the orchestrator; no findings emitted due to allowlist overreach; no side effects) and NOT marked PASS (findings were not emitted) and NOT marked FAIL (no unsafe output, no project/candidate mutation, no Memory/Canon mutation, no promotion, no apply-promotion, no story prose). T018C did not edit the orchestrator, the preflight, tests, frontend, package/dependency files, or `.external_sources/`; did not run `npm install`, `npm audit fix`, start a Node server, run `npm run validate:file` over project data, mutate Memory/Canon, create promotion records, run apply-promotion, or generate story prose; did not stage, commit, or push. Untracked evidence under `.codex-context/PHASE8-IMPL-023/manual-validation/T018C-ncp/` (intentionally not staged). T018C was followed by T018C1 (allowlist repair, complete/PASS) and then T018C-rerun (validation rerun, complete/PASS); the T018C `PASS-SAFE-FAIL-CLOSED` history is preserved unchanged. The T018A npm audit caveat (`ajv` moderate, `fast-uri` high) remains a recorded, owner-known evidence note.
- Previous child implementation: `PHASE8-IMPL-023-T018B - NCP candidate-import validation adapter` is complete/PASS. Decision record: `docs/roadmap/decisions/PHASE8-IMPL-023-T018B-ncp-candidate-import-validation-adapter.md`. T018B adds `_build_ncp_live_runner` to `backend/omi_analysis_orchestrator.py`, gated behind `OMI_LIVE_TOOLS_ENABLED=1` + `OMI_LIVE_NCP_ENABLED=1` + not `OMI_LIVE_NCP_BLOCKED` + an explicit `OMI_LIVE_NCP_INPUT_PATH` (and, when set, an opt-in `OMI_LIVE_NCP_VALIDATE_WITH_NODE=1` whose subprocess step is mocked in tests). The runner reads the owner-selected NCP JSON file, performs a minimal Python-side schema/readiness check (never `npm`, never a Node validator), and maps a small, evidence-backed subset of NCP subtext/storytelling containers (`players` -> `evidence_note`, `storypoints`/`storybeats`/`appreciations` -> `story_fact`, `dynamics`/`vectors` -> `open_question`, `overviews` -> `throughline_context`, `relationships` -> `relationship`, `open_questions` -> `open_question`, `diagnostic_questions` -> `diagnostic_question`, `story.moments` -> `story_fact`) into the existing T009 `omi_ncp_context_handoff.v1` envelope shape; the T009 fixture envelope validator is authoritative. Every finding carries a `source_excerpt` (capped at 240 chars; never rewritten/sanitized), a `source_locator` as an RFC 6901 JSON pointer, provenance `tool_source: "ncp"` with support-only label, `owner_decision: pending`, and `review_status: "candidate_review_pending"`. The runner is fail-closed on missing/empty/unsafe/unreadable/invalid-JSON input and on every T009 validation failure. The path-allowlist resolver rejects traversal, symlinks (before AND after `Path.resolve`), directories, hidden unsafe locations, and any path with a `projects` / `artifacts` / `graphify-out` / `ai_context` / `.codex-context` segment; allowed roots are `tests/`, `tempfile.gettempdir()`, and `.external_sources/`. `backend/omi_runtime_preflight.py` (T018A follow-up) now takes an optional `env` argument on `_ncp_runtime_probe` and surfaces the T018B configuration-only fields `ncp_input_path_env`, `ncp_input_path_configured`, `ncp_input_path_value`, `ncp_validate_with_node_env`, `ncp_validate_with_node_enabled` (the preflight does NOT read the NCP file or invoke Node/npm based on these env vars). 18 new mocked T018B tests in `tests/test_omi_ncp_subtxt_dramatica_flow_adapter_contract.py` and 4 new T018B preflight tests in `tests/test_omi_live_runtime_preflight_contract.py`. `tests/test_omi_ncp_subtxt_dramatica_flow_adapter_contract.py` -> 49 passed (31 existing T009 + 18 new T018B); `tests/test_omi_tool_assisted_orchestrator_contract.py` -> 30 passed; `tests/test_omi_tool_assisted_persistence_contract.py` -> 5 passed; `tests/test_omi_live_runtime_preflight_contract.py` -> 72 passed (68 existing T018A + 4 new T018B). `python3 -m py_compile backend/omi_analysis_orchestrator.py backend/omi_runtime_preflight.py backend/main.py backend/analysis_engine.py` -> exit 0. No real `npm install`, no real `npm audit fix`, no real Node server, no real `npm run validate:file` over project data, no Memory/Canon mutation, no automatic promotion records, no automatic apply-promotion, no NCP candidate persistence, no story prose; no real `.external_sources/narrative-context-protocol/examples/*` NCP file was used by the T018B tests (all 18 new tests use small NCP JSON files written to `tmp_path`). The known `ajv` moderate / `fast-uri` high npm audit caveat remains a recorded, owner-known evidence note. T018B completes through mocked automated adapter tests and preflight/docs/status; manual real NCP validation against an owner-selected real NCP JSON file is intentionally deferred to `PHASE8-IMPL-023-T018C`. `PHASE8-IMPL-023-T018C1 - Repair _ncp_resolve_allowed_input_path overzealous 'for part in resolved.parts: if part == "projects"' segment walk` is the recommended next task.
- Earlier child implementation: `PHASE8-IMPL-023-T018A - NCP schema-validator preflight` is complete/PASS. Decision record: `docs/roadmap/decisions/PHASE8-IMPL-023-T018A-ncp-schema-validator-preflight.md`. T018A extends the T013 read-only runtime preflight in `backend/omi_runtime_preflight.py` so the OMI `runtime-preflight` report for the `ncp` tool carries a focused, read-only NCP schema-validator surface (`ncp_runtime_surface`, `ncp_source_path`, `ncp_source_available`, `ncp_package_json_available`, `ncp_schema_json_available`, `ncp_schema_yaml_available`, `ncp_validate_schema_script_available`, `ncp_validate_file_script_available`, `ncp_validate_schema_package_script_available`, `ncp_validate_file_package_script_available`, `ncp_node_available`, `ncp_node_path`, `ncp_npm_available`, `ncp_npm_path`, `ncp_node_modules_available`, `ncp_validator_available`, `ncp_validator_status`, `ncp_audit_caveat`, `ncp_detail`). The probe is read-only, fail-closed, never runs `npm install`, `npm audit fix`, `npm run validate:schema`, or `npm run validate:file`, never imports NCP as a Python module, and never exposes NCP as a network/server path. The probe records the known npm audit caveat (`ajv` moderate direct dep, `fast-uri` high transitive dep) as known owner evidence. NCP remains a schema/interchange validation surface, not an automatic analysis runtime, not canon/truth. NCP candidate-import validation is intentionally deferred to `PHASE8-IMPL-023-T018B`. The existing live-tool flag map (`OMI_LIVE_NCP_ENABLED` / `OMI_LIVE_NCP_BLOCKED` / `OMI_LIVE_NCP_BLOCKED_REASON`) is preserved; NCP stays disabled by default. 18 new mocked tests in `tests/test_omi_live_runtime_preflight_contract.py` cover disabled-by-default safety, all-surfaces-available reporting, missing source/package.json/schema JSON/schema YAML/validate-schema.js/validate-file.js/validate:schema package script/validate:file package script/node/npm, existing `node_modules` reporting, `node_modules` missing (degraded), blocked-flag override, no-`npm install`/no-`npm audit fix`/no-`npm run validate:schema`/no-`npm run validate:file`, no persistence / no Memory/Canon mutation / no apply-promotion / no story prose, and audit-caveat reporting. `tests/test_omi_live_runtime_preflight_contract.py` -> 68 passed (50 existing + 18 new T018A); `tests/test_omi_ncp_subtxt_dramatica_flow_adapter_contract.py` -> 31 passed; `tests/test_omi_tool_assisted_orchestrator_contract.py` -> 30 passed; `tests/test_omi_tool_assisted_persistence_contract.py` -> 5 passed. `python3 -m py_compile backend/omi_runtime_preflight.py backend/omi_analysis_orchestrator.py backend/main.py backend/analysis_engine.py` -> exit `0`. Real-machine preflight validation on the owner machine confirms the probe correctly reports the existing NCP source tree at `.external_sources/narrative-context-protocol` with `node` 24.16.0, `npm` 11.13.0, and `node_modules` all present. No candidate persistence, no Memory/Canon mutation, no automatic promotion record, no automatic apply-promotion, no story prose; no `npm install` / `npm audit fix` / `npm run validate:schema` / `npm run validate:file` was run. `backend/omi_analysis_orchestrator.py` was not modified. `PHASE8-IMPL-023-T018B - NCP candidate-import validation adapter` is the recommended next task.
- Previous child implementation: `PHASE8-IMPL-023-T017C1 - BookNLP / transformers state-dict compatibility repair` is complete/PASS. Decision record: `docs/roadmap/decisions/PHASE8-IMPL-023-T017C1-booknlp-transformers-state-dict-compatibility-repair.md`. T017C1 repairs the T017C `RuntimeError: Unexpected key(s) in state_dict: "bert.embeddings.position_ids"` with a narrow adapter-side shim in `backend/omi_analysis_orchestrator.py` that wraps `torch.load` for the duration of the BookNLP constructor only and drops ONLY the single known-incompatible non-trainable key `bert.embeddings.position_ids`; `load_state_dict(strict=True)` is preserved and the shim is restored in a `try/finally` block. No dependency change was applied (`booknlp==1.0.8`, `transformers==5.5.0`, `torch==2.10.0+cu129`, `spacy==3.8.14`, `tensorflow==2.21.0`, `setuptools==80.9.0`, `huggingface-hub==1.16.1`); the transformers pin Option A was rejected as unsafe because it would force churn across the `unsloth` / `peft` / `trl` training stack. Manual real OMI live BookNLP re-validation against `projects/example/scenes/scene_001.md` with the same T017C runtime flags (`OMI_LIVE_TOOLS_ENABLED=1 OMI_LIVE_BOOKNLP_ENABLED=1 OMI_LIVE_BOOKNLP_MODEL=small OMI_LIVE_BOOKNLP_PIPELINE=entity,quote,supersense,event`) and `persist_candidates=False` now returns `analysis_status: "succeeded"`, `adapter_results[booknlp].state: "succeeded"`, `finding_count: 26`, `finding_types: ["character", "location"]`, `persisted_candidate_ids: []`, `candidate_paths_changed: false`, `project_files_changed: false`, and all 11 safety envelope flags true. Every finding carries `provenance.tool_source="booknlp"`, `provenance.adapter="booknlp"`, `provenance.support="BookNLP live support only"`, `owner_decision: pending`, `review_status: "candidate_review_pending"`, and a `source_excerpt` + `source_locator` evidence pair. 9 new mocked tests in `tests/test_omi_booknlp_spacy_adapter_contract.py` (shim filter function, install/uninstall roundtrip, position_ids removal, arbitrary unexpected key preservation, non-dict pass-through, dict no-tolerated-key pass-through, scoped to constructor, strict=True preservation, safety boundaries preserved). All existing T017B mocked tests still pass. The only failing test in the suite is the pre-existing `test_live_spacy_missing_package_returns_unavailable` which fails because `spacy` is installed locally and is unrelated to T017C1. `tests/test_omi_booknlp_spacy_adapter_contract.py` -> 39 passed (30 existing + 9 new T017C1), `tests/test_omi_tool_assisted_orchestrator_contract.py` -> 30 passed, `tests/test_omi_tool_assisted_persistence_contract.py` -> 5 passed, `tests/test_omi_live_runtime_preflight_contract.py` -> 50 passed. Captured to `.codex-context/PHASE8-IMPL-023/manual-validation/T017C1-booknlp-repair/manual-booknlp-repair-validation-output.txt`. Recommended next task: `PHASE8-IMPL-023-T018A - NCP schema validator preflight`. No Memory/Canon mutation, no promotion record, no apply-promotion, no canon promotion, no story prose, no package install/upgrade, no `strict=False` broad workaround, no patch to installed site-packages, no edit to `.external_sources/`, no project/candidate persistence, no staging, no commit, no push.

- Latest child implementation: `PHASE8-IMPL-023-T017C - Manual real BookNLP validation` is complete/PASS-SAFE-FAIL-CLOSED. Decision record: `docs/roadmap/decisions/PHASE8-IMPL-023-T017C-manual-real-booknlp-validation.md`. T017C ran the T017B live `booknlp` adapter end-to-end through `analyze_omi_raw_idea_with_tools` against the installed `.venv-unsloth-clean` BookNLP runtime (`booknlp==1.0.8`, `transformers==5.5.0`, `torch==2.10.0+cu129`, `spacy==3.8.14`, `tensorflow==2.21.0`, `setuptools==80.9.0`) on owner-authored project fixture `projects/example/scenes/scene_001.md` (1971 chars, "The Princess and the Pea" public-domain scene). Runtime flags: `OMI_LIVE_TOOLS_ENABLED=1 OMI_LIVE_BOOKNLP_ENABLED=1 OMI_LIVE_BOOKNLP_MODEL=small OMI_LIVE_BOOKNLP_PIPELINE=entity,quote,supersense,event`; `OMI_LIVE_BOOKNLP_BLOCKED` was not set; `persist_candidates=False`. BookNLP was actually reached: `booknlp.booknlp.BookNLP` was imported lazily inside `_build_booknlp_live_runner`, the BookNLP entity Tagger / coref / speaker model files were downloaded from the Hugging Face Hub on first run, the entity Tagger was instantiated, and `Tagger.load_state_dict(...)` raised `RuntimeError: Error(s) in loading state_dict for Tagger: Unexpected key(s) in state_dict: "bert.embeddings.position_ids"` — a `transformers==5.5.0` vs `booknlp==1.0.8` model state-dict compatibility error (newer `transformers` adds a `bert.embeddings.position_ids` buffer that the older BookNLP model checkpoint does not have). The live runner caught the `RuntimeError` and returned the T017B fail-closed envelope: `state: "failed_closed"`, `candidates_count: 0`, `analysis_status: "fail_closed"`, all 11 safety envelope flags except `no_real_tool_calls` (which the live path flips to `false` as designed) returned `true` (`no_prose`, `no_memory_canon_mutation`, `no_apply_promotion`, `no_canon_promotion`, `no_package_installs`, `no_story_prose_generation`, `candidate_presence_is_not_canon`, `queue_presence_is_not_approval`, `support_is_not_truth`, `tool_output_is_not_canon`). `persist_candidates=False` was honored (`persistence_status: "not_requested"`, `persisted_candidate_ids: []`, `new_candidate_ids: []`, `reused_candidate_ids: []`); no candidate files were written to `projects/example/omi/candidates/`, no `projects/example/omi/index.json` mutation, no `projects/example/omi/promotions/` file change. No Memory/Canon mutation, no automatic promotion records, no automatic apply-promotion, no story prose. Combined automated validation: `tests/test_omi_booknlp_spacy_adapter_contract.py` -> 30 passed, 1 failed (pre-existing `test_live_spacy_missing_package_returns_unavailable` unrelated to T017C; spaCy is now installed locally in `.venv-unsloth-clean`); `tests/test_omi_tool_assisted_orchestrator_contract.py` -> 30 passed; `tests/test_omi_tool_assisted_persistence_contract.py` -> 5 passed; `tests/test_omi_live_runtime_preflight_contract.py` -> 50 passed. `python3 -m py_compile backend/omi_analysis_orchestrator.py backend/omi_runtime_preflight.py backend/main.py backend/analysis_engine.py` -> exit `0`. `python3 -m json.tool docs/roadmap/enrichment/PHASE8-IMPL-023.enrichment.json` -> VALID. T017A/T017B runtime caveats (CPU fallback, `setuptools==80.9.0`/`pkg_resources` pin, `torch==2.10.0+cu129` C++ extension warning, first-run model download) remain in effect; the proximate cause of the T017C PASS-SAFE-FAIL-CLOSED outcome is the new `transformers` `bert.embeddings.position_ids` state-dict key. No backend/frontend/test/package/dependency file was edited in T017C; no git add/commit/push was performed. `PHASE8-IMPL-023-T017C1` is the recommended next task as a narrowly scoped repair task to address the `transformers`/BookNLP model state-dict compatibility and rerun T017C.

- Previous child implementation: `PHASE8-IMPL-023-T017B - Live BookNLP adapter behind flags` is complete/PASS. Decision record: `docs/roadmap/decisions/PHASE8-IMPL-023-T017B-live-booknlp-adapter-behind-flags.md`. T017B adds `_build_booknlp_live_runner` to `backend/omi_analysis_orchestrator.py`. The runner imports `booknlp.booknlp.BookNLP` lazily (never at module import time) and is gated behind `OMI_LIVE_TOOLS_ENABLED` + `OMI_LIVE_BOOKNLP_ENABLED` + not `OMI_LIVE_BOOKNLP_BLOCKED` env flags; disabled by default. The runner writes the owner raw idea to a temporary `omi_booknlp_input.txt` file under `tempfile.TemporaryDirectory(prefix="omi_booknlp_live_")`, runs `BookNLP("en", model_params).process(...)` against the temporary output directory, parses the temporary `.entities` / `.quotes` / `.tokens` files, and converts the parsed rows into the existing T007 `omi_booknlp_local_nlp_extraction.v1` envelope shape through `_booknlp_live_result_to_envelope`. The T007 `validate_local_nlp_fixture_envelope` validator is authoritative and remains unchanged. Normalized T017B outputs: `.entities` rows -> `character`/`location`/`organization`/`object` candidates (PER/GPE/LOC/FAC/ORG/VEH); `.quotes` rows -> `diagnostic_question` candidates; `.tokens` rows used for byte-offset locators. Deferred: EVENT/supersense, coreference/alias truth, referential gender/pronoun, raw BookNLP artifact persistence. Fail-closed on ImportError, OSError, generic Exception, blocked flag, empty/malformed output, unsafe text, and T007 validation failure; the runner does NOT rewrite or sanitize unsafe text. No raw BookNLP output is persisted to project storage. 13 new mocked-BookNLP tests in `tests/test_omi_booknlp_spacy_adapter_contract.py`; all tests mock the BookNLP module via `sys.modules` monkeypatching and do not require a real BookNLP runtime. `tests/test_omi_booknlp_spacy_adapter_contract.py` -> 30 passed. `tests/test_omi_tool_assisted_orchestrator_contract.py` -> 30 passed. `tests/test_omi_tool_assisted_persistence_contract.py` -> 5 passed. `tests/test_omi_live_runtime_preflight_contract.py` -> 50 passed. `python3 -m py_compile backend/omi_analysis_orchestrator.py backend/omi_runtime_preflight.py backend/main.py backend/analysis_engine.py` -> exit 0. No live BookNLP call, no Memory/Canon mutation, no automatic promotion records, no automatic apply-promotion, no story prose, no raw BookNLP output persisted to project storage. T017A runtime caveats (CPU fallback, `setuptools==80.9.0`/`pkg_resources` pin, `torch==2.10.0+cu129` C++ extension warning) remain in effect. T017C (manual real BookNLP validation on owner-authored text) is the recommended next task.

- T016D result: `PHASE8-IMPL-023-T016D - Manual real Story Check validation` is complete/PASS.
- T016B result: `PHASE8-IMPL-023-T016B - Story Check runtime preflight/config/availability check` is complete/PASS. Decision record: `docs/roadmap/decisions/PHASE8-IMPL-023-T016B-story-check-runtime-preflight-config-availability.md`. T016B strengthens the T013 `story_check` preflight probe from a single file-existence check on `backend/analysis_engine.py` to a read-only multi-surface probe in `backend/omi_runtime_preflight.py` (`_story_check_runtime_probe` + `_dependency_probe` + `_tool_report`). The probe now reports the boolean availability of `backend/analysis_engine.py`, `backend/prompts/story_check.txt`, `backend/mock_responses/story_check.json`, `backend/analysis_modes.py`, `backend/storyform.py`, and the `requests` Python package (via `importlib.util.find_spec`), and surfaces the configured `OLLAMA_BASE_URL`/`OLLAMA_MODEL`/`OLLAMA_TIMEOUT_SECONDS`/`ANALYSIS_MODE` env vars as configuration only. T016B does not import `backend.analysis_engine`, does not call `analysis_engine.run_story_check`, does not call the legacy Story Check route, and does not call Ollama. The probe reuses the existing T013 status vocabulary (`enabled`/`disabled`/`available`/`unavailable`/`blocked`/`error`/`not_configured`) and preserves the existing disabled-by-default and blocked-state behavior of the T013 preflight. New reported fields on the `story_check` tool entry: `story_check_runtime_surface`, `story_check_analysis_engine_available`, `story_check_prompt_available`, `story_check_mock_fixture_available`, `story_check_analysis_modes_available`, `story_check_storyform_surface_available`, `story_check_requests_available`, `story_check_ollama_base_url`, `story_check_ollama_model_name`, `story_check_ollama_timeout_seconds`, `story_check_analysis_mode_value`, `story_check_analysis_mode_configured`, and `story_check_detail`. Mock fixture is treated as a partial-availability signal only: when the mock fixture is missing but the other required surfaces are present, the probe remains `enabled`/`available` per the normal T013 flow with `story_check_mock_fixture_available: False` and a `detail` that explicitly mentions `mock_responses/story_check.json`. T016B adds 16 new mocked tests in `tests/test_omi_live_runtime_preflight_contract.py` (all five surfaces ready, disabled-by-default safe, live-enabled-but-unconfigured, blocked override, missing prompt, missing mock fixture (partial), missing `requests`, missing analysis engine, no-`run_story_check`-call, no-Ollama-or-network, env-var surfacing, invalid `ANALYSIS_MODE`, invalid `OLLAMA_TIMEOUT_SECONDS`, missing storyform+analysis_modes, no-persist-or-promote). All 39 preflight tests pass (23 existing T013/T014B/T015B + 16 new T016B). All 46 fixture/orchestrator/persistence tests pass unchanged. `python3 -m py_compile backend/omi_runtime_preflight.py backend/main.py backend/omi_analysis_orchestrator.py` exits 0. No live Story Check runtime call, no live Story Check adapter implementation, no model analysis, no candidates, no candidate persistence, no Memory/Canon mutation, no automatic promotion records, no automatic apply-promotion, no story prose, no backend/frontend/package/dependency file changes occurred. T016C (live Story Check adapter behind flags) is the recommended next step; T016C should pick option (A) from the T016A decision record (bridge to `backend.analysis_engine.run_story_check` with a new `OMI_LIVE_STORY_CHECK_SCENE_ID` env var) or document another path.
- T015D-rerun-after-T015F result: `PHASE8-IMPL-023-T015D-rerun-after-T015F - Rerun manual real Ollama qwen3:8b validation after T015F` is complete/PASS. Decision record: `docs/roadmap/decisions/PHASE8-IMPL-023-T015D-manual-real-ollama-qwen3-validation.md` (this rerun is documented in the same file as a T015D rerun history section). T015D was rerun after T015E added top-level `"think": false` to the live `/api/chat` request body and T015F narrowed the orchestrator-level no-prose guard scope and added the `OMI_LIVE_OLLAMA_TIMEOUT_SECONDS` env var (default `180`). T015D rerun #2 confirmed: Ollama HTTP API reachable from WSL at `http://172.25.144.1:11434` (version 0.31.2); `qwen3:8b` confirmed available via `/api/tags`. Live Ollama adapter correctly reached behind env flags (`OMI_LIVE_TOOLS_ENABLED=1 OMI_LIVE_OLLAMA_ENABLED=1 OMI_LIVE_OLLAMA_MODEL=qwen3:8b OMI_LIVE_OLLAMA_TIMEOUT_SECONDS=180 OMI_LIVE_OLLAMA_BASE_URL=http://172.25.144.1:11434`) and the 35-word narrative test text suggested in the task brief was accepted at the orchestrator entrypoint (T015F owner-input prose guard repair exercised end-to-end) and reached the live adapter as a user message. The full live call completed in ~99 seconds (`time.monotonic()` measured `98.93s`), well within the 180s default timeout and well above the previous 30s hard-coded limit. The adapter received a valid `omi_ollama_structured_extraction.v1` JSON envelope in `message.content` with `done_reason: "stop"` and produced 3 evidence-backed candidate findings (1 timeline_event, 1 object, 1 organization) with `source_excerpt`, `source_locator`, `provenance.tool_source`, support-only labels, `owner_decision: pending`, and `review_status: "candidate_review_pending"`. `persist_candidates=False` was honored (`persistence_status: "not_requested"`, `new_candidate_ids: []`, `persisted_candidate_ids: []`). All 100 automated tests passed across the four contract test files. No Memory/Canon mutation, no automatic promotion records, no apply-promotion, and no story prose occurred; all 11 safety envelope flags returned `true`. T015E `think: false` payload is preserved at `backend/omi_analysis_orchestrator.py:4602-4608`; T015F `OMI_LIVE_OLLAMA_TIMEOUT_SECONDS` is read at `backend/omi_analysis_orchestrator.py:4589-4592` and applied at `4617`; T015F `validate_owner_raw_idea_input` helper is the documented owner-input boundary. No backend, frontend, package, or dependency files were changed during T015D rerun #2. T015D is now complete/PASS for real Ollama qwen3:8b validation; T015E and T015F code/test changes are pre-existing.
- T016C result: `PHASE8-IMPL-023-T016C - Live Story Check adapter behind flags` is complete/PASS. Decision record: `docs/roadmap/decisions/PHASE8-IMPL-023-T016C-live-story-check-adapter-behind-flags.md`. T016C adds `_build_story_check_live_runner` to `backend/omi_analysis_orchestrator.py`, which imports `backend.analysis_engine.run_story_check` lazily and bridges the OMI orchestrator to the existing in-repo `run_story_check(project_name, scene_id)` callable surface. The live path is gated behind `OMI_LIVE_TOOLS_ENABLED` + `OMI_LIVE_STORY_CHECK_ENABLED` + not `OMI_LIVE_STORY_CHECK_BLOCKED` env flags and is disabled by default. The orchestrator entrypoint `analyze_omi_raw_idea_with_tools` gains a new optional kwarg `story_check_scene_id: str | None = None` that wins over the new `OMI_LIVE_STORY_CHECK_SCENE_ID` env var when both are present; missing both returns `unavailable` with no live call and never invents a `scene_id`. The runner converts the legacy rich-Story-Check response (warnings/concerns/suggestions/insufficient_evidence/throughline_alignment/theme_drift/character_consistency) into the existing T008 `omi_story_check_diagnostic_handoff.v1` envelope shape through `_story_check_result_to_envelope` and sanitizes forbidden `truth|canon|final|approved|promoted` labels in legacy text via `_story_check_sanitize_truth_final_labels` so the existing T008 `validate_story_check_fixture_envelope` validator (authoritative) accepts the converted envelope. The T008 validator still rejects prose-intent field names, truth/canon/final/approved labels, Memory/Canon mutation, candidate persistence, promotion records, apply-promotion requests, prose-shaped claims, and non-`pending` owner decisions. Fail-closed on `ImportError`, runtime exceptions, missing context, blocked flag, malformed legacy output, unsafe converted envelope, and free-form/prose-only legacy output. 14 new mocked tests in `tests/test_omi_story_check_adapter_contract.py` (live disabled by default, live enabled with mocked runtime success, env scene id fallback, explicit scene id override, missing scene id, blocked flag override, runtime exception, malformed legacy result, prose-only legacy result, unsafe output via T008 validator, legacy error shape, existing fixture tests still pass, persistence boundary, no legacy route call, no live call when flags off). `tests/test_omi_story_check_adapter_contract.py` -> `25 passed` (11 existing T008 + 14 new T016C). `tests/test_omi_tool_assisted_orchestrator_contract.py` -> `30 passed`. `tests/test_omi_tool_assisted_persistence_contract.py` -> `5 passed`. `tests/test_omi_live_runtime_preflight_contract.py` -> `39 passed` (T016C is not a preflight task). Combined T016C validation: `99 passed` across the four contract test files. `python3 -m py_compile backend/omi_analysis_orchestrator.py backend/omi_runtime_preflight.py backend/main.py backend/analysis_engine.py` -> exit `0`. T016C does not call the legacy `POST /api/projects/{project_name}/story-check/{scene_id}` route, does not call Ollama directly (the `analysis_engine.run_story_check` callable surface owns the Ollama HTTP behavior), does not mutate Memory/Canon, does not create promotion records, does not run apply-promotion, does not persist candidates, and does not generate story prose. No frontend/package/dependency file was changed. T016D (manual real Story Check validation against a live `qwen3:8b` instance behind the new env flags) is the future manual real validation task. T016C bridges option (A) from the T016A decision record: orchestrator live runner -> existing `backend.analysis_engine.run_story_check` -> convert legacy response -> existing T008 fixture validator.
- T016A result: `PHASE8-IMPL-023-T016A - Story Check runtime surface and fixture contract inspection` is complete/PASS as inspect/docs only. Decision record: `docs/roadmap/decisions/PHASE8-IMPL-023-T016A-story-check-runtime-surface-and-fixture-contract-inspection.md`. T016A mapped the existing T008 `story_check` fixture/mock contract (`omi_story_check_diagnostic_handoff.v1` schema, 12 supported finding types, strict evidence/provenance/source-locator/no-prose/no-truth/no-final/no-operation/no-approval/no-Memory-Canon-mutation/no-promotion/no-apply-promotion guards, all 11 fixture/mock tests in `tests/test_omi_story_check_adapter_contract.py`) at `backend/omi_analysis_orchestrator.py:2550-3264`. T016A mapped the existing in-repo Story Check runtime surface as `backend/analysis_engine.run_story_check(project_name, scene_id)` at `backend/analysis_engine.py:46` invoked through the legacy `POST /api/projects/{project_name}/story-check/{scene_id}` route at `backend/main.py:327-332`; the legacy route is not gated by OMI live-tools env flags and will perform a real `requests.post` to `OLLAMA_BASE_URL/api/chat` (default `http://localhost:11434`) if `ANALYSIS_MODE=ollama_baseline`; the orchestrator side does not import or call `analysis_engine.run_story_check`. T016A mapped the existing T013 preflight `story_check` probe as a file-existence-only probe on `backend/analysis_engine.py` at `backend/omi_runtime_preflight.py:280-283`; the probe does not check `requests` package, prompt file, mock fixture, `analysis_modes` env-var validity, or Ollama HTTP reachability. T016A documented three next slices: T016B (live Story Check preflight/config/availability with `requests`/`prompt`/`mock fixture`/`analysis_modes`/`Ollama reachability`/`timeout` probes and `OMI_LIVE_STORY_CHECK_BLOCKED`/`OMI_LIVE_STORY_CHECK_BLOCKED_REASON` support); T016C (live Story Check adapter behind flags — choose between (A) bridge to `analysis_engine.run_story_check`, (B) fresh orchestrator HTTP runner, or (C) refactor; convert rich Story Check response to T008-shaped `omi_story_check_diagnostic_handoff.v1` findings; fail-closed on HTTP error, non-JSON, prose, truth, canon, missing evidence/source_locator/provenance); T016D (manual real Story Check validation against live `qwen3:8b`). T016B is the recommended next task after T016A. No live Story Check runtime call, no adapter implementation, no model analysis, no candidates, no candidate persistence, no Memory/Canon mutation, no automatic promotion records, no automatic apply-promotion, no story prose, no backend/frontend/package/dependency file changes occurred. Existing Story Check tests remain scaffolding only and do not count as MVP completion.
- T015F result: `PHASE8-IMPL-023-T015F - Narrow no-prose guard scope and configure live Ollama timeout` is complete/PASS. Decision record: `docs/roadmap/decisions/PHASE8-IMPL-023-T015F-narrow-no-prose-guard-and-configure-ollama-timeout.md`. T015F repairs the two T015D-rerun blockers: (1) the orchestrator-entrypoint no-prose guard was incorrectly rejecting owner-authored raw idea input as prose-like, and (2) the live Ollama HTTP timeout was hard-coded at 30 seconds while `qwen3:8b` with the full T006 system prompt takes ~111s on the owner's hardware. T015F narrows the no-prose guard scope: the strict prose guard is preserved on AI/tool/model `extracted_claim` output and on forbidden envelope field names (rewrite, continue, outline, draft, polish, improve, expand, imitate, revise, etc.), but is intentionally NOT applied to owner-authored raw idea input, which is treated as untrusted data to analyze. T015F adds `validate_owner_raw_idea_input` helper to `backend/omi_analysis_orchestrator.py` and removes the `is_prose_like_text(raw_idea_text)` fail-closed block in `analyze_omi_raw_idea_with_tools`. T015F adds the `OMI_LIVE_OLLAMA_TIMEOUT_SECONDS` env var (default `180`, clamped to a finite positive range) and a `_env_positive_float` helper that falls back to the default for missing, blank, non-numeric, zero, negative, non-finite, sub-min, or over-max values. `_build_ollama_model_live_runner` reads the configured timeout and passes it to `urllib.request.urlopen(..., timeout=...)`. The T015E `think: false` payload is preserved unchanged. T015F adds 9 new mocked-HTTP tests in `tests/test_omi_ollama_model_adapter_contract.py` (default 180s timeout, env override, invalid values fall back to default, missing env, `think: false` preserved, owner prose input accepted, model prose output still fails closed, model truth-label output still fails closed, persistence boundary remains safe) and 4 new tests in `tests/test_omi_tool_assisted_orchestrator_contract.py` (owner prose input accepted, `validate_owner_raw_idea_input` helper, model prose output still fails closed, model truth-label output still fails closed). One existing test is updated to reflect the narrowed prose-guard scope. All tests mock `urllib.request.urlopen`; no real Ollama/network call is required. `tests/test_omi_ollama_model_adapter_contract.py` -> `42 passed` (33 existing + 9 new T015F). `tests/test_omi_tool_assisted_orchestrator_contract.py` -> `30 passed` (26 existing + 4 new T015F; 1 existing test updated). `tests/test_omi_tool_assisted_persistence_contract.py` -> `5 passed` (unchanged). `tests/test_omi_live_runtime_preflight_contract.py` -> `23 passed` (T015F is not a preflight task). Combined T015F validation: `100 passed` across the four contract test files. `python3 -m py_compile backend/omi_analysis_orchestrator.py backend/omi_runtime_preflight.py backend/main.py` -> exit `0`. No real `/api/chat` or `/api/generate` call was made in this task. No frontend/package/dependency files changed. No candidate persistence, Memory/Canon mutation, automatic promotion records, automatic apply-promotion, or story prose behavior was added. The T006 envelope validator remains the authoritative validator; the structured JSON mode/schema `format` field is intentionally deferred to a future task. T015D must be rerun after T015F to validate real `qwen3:8b` output through the orchestrator path with the narrowed prose-guard scope and the 180s default timeout.
- T015E result: `PHASE8-IMPL-023-T015E - Disable Qwen thinking mode for live Ollama structured extraction` is complete/PASS. Decision record: `docs/roadmap/decisions/PHASE8-IMPL-023-T015E-disable-qwen-thinking-for-ollama-structured-extraction.md`. T015E is the focused T015C live Ollama adapter fix that disables Qwen3 thinking-mode output by adding a single top-level field `"think": false` to the `/api/chat` request body built by `_build_ollama_model_live_runner` in `backend/omi_analysis_orchestrator.py`, alongside the existing `stream=false` and `options.num_predict=2048`. The change is intentionally not behind a new opt-in flag; for live structured extraction, thinking output must be disabled by default so the adapter receives JSON in `message.content`. The adapter continues to parse extraction JSON only from `message.content`; `message.thinking` is never read and is not treated as extraction output. The owner confirmed that without `think: false` the `/api/chat` response returns `message.content: ""` and `message.thinking: <trace>` with `done_reason: "length"`, and that with `think: false` the response returns the expected JSON in `message.content` with `done_reason: "stop"`. T015E adds 3 new mocked-HTTP tests in `tests/test_omi_ollama_model_adapter_contract.py`: (1) the request payload includes top-level `think: false`, (2) the adapter extracts JSON only from `message.content` and ignores a valid-looking envelope embedded in `message.thinking`, and (3) the exact Qwen3 thinking-mode failure shape (empty `message.content`, non-empty `message.thinking`, `done_reason: "length"`) fails closed with no findings. All tests mock `urllib.request.urlopen`; no real Ollama/network call is required. `tests/test_omi_ollama_model_adapter_contract.py` -> `33 passed` (30 existing + 3 new T015E). `tests/test_omi_live_runtime_preflight_contract.py` -> `23 passed` (T015E is not a preflight task; the preflight test file was not modified). `tests/test_omi_tool_assisted_orchestrator_contract.py` -> `26 passed`. `tests/test_omi_tool_assisted_persistence_contract.py` -> `5 passed`. `python3 -m py_compile backend/omi_analysis_orchestrator.py backend/omi_runtime_preflight.py backend/main.py` -> exit `0`. No real `/api/chat` or `/api/generate` call was made in this task. No frontend/package files changed. No candidate persistence was invoked. No Memory/Canon mutation, automatic promotion records, automatic apply-promotion, or story prose behavior was added. T015D must be rerun after T015E to validate real `qwen3:8b` output with `think: false` enabled; T015D final result is NOT marked PASS in this task.
- T015C result: `PHASE8-IMPL-023-T015C - Live Ollama structured extraction adapter behind flags` is complete/PASS. Decision record: `docs/roadmap/decisions/PHASE8-IMPL-023-T015C-live-ollama-structured-extraction-adapter-behind-flags.md`. T015C adds `_build_ollama_model_live_runner` to `backend/omi_analysis_orchestrator.py`, using `urllib.request` only to call Ollama `/api/chat` with strict JSON extraction instructions. Wired into `_resolve_adapter_runner` behind `OMI_LIVE_TOOLS_ENABLED` + `OMI_LIVE_OLLAMA_ENABLED` + not `OMI_LIVE_OLLAMA_BLOCKED`. Env vars: `OMI_LIVE_OLLAMA_BASE_URL` (default `http://127.0.0.1:11434`), `OMI_LIVE_OLLAMA_MODEL` (default `qwen3:8b`), with `OMI_LIVE_OLLAMA_MODEL_NAME` compatibility. Fail-closed on HTTP errors, non-JSON, prose, unsafe/truth/canon output. Tests mock `urllib.request.urlopen`; no real Ollama required. Tests: `tests/test_omi_ollama_model_adapter_contract.py` -> `30 passed` (19 existing + 11 new). No live `/api/chat` or `/api/generate` call was made during tests or implementation. No frontend/package files changed. T015E is the focused Qwen thinking-mode fix on top of T015C.
- T015D result: `PHASE8-IMPL-023-T015D - Manual real Ollama qwen3:8b validation` is complete/PASS-SAFE-FAIL-CLOSED. Decision record: `docs/roadmap/decisions/PHASE8-IMPL-023-T015D-manual-real-ollama-qwen3-validation.md`. T015D ran manual real Ollama `qwen3:8b` validation from WSL against Windows-hosted Ollama at `http://172.25.144.1:11434`. Ollama API version endpoint and `/api/tags` were reachable; `qwen3:8b` confirmed available with tools capability. The live Ollama adapter was correctly reached but the model failed to load (HTTP 500) because the Windows Ollama installation is missing `llama-server.exe`. The adapter correctly failed closed with no findings, no persistence, no Memory/Canon mutation, no promotion records, no apply-promotion, no story prose. `persist_candidates=False` was honored. All 84 automated tests passed. The prose guard blocked the suggested 36-word narrative input as prose-like (text >= 24 words ending with `.`). No backend/frontend/package/dependency files were changed. The owner has since repaired the Windows Ollama installation (ollama 0.31.2; `qwen3:8b` present; WSL reachability re-confirmed) and confirmed the remaining issue is Qwen3 thinking-mode output consuming the response budget; T015E is the focused fix on top of T015C, and T015D must be rerun after T015E to validate real `qwen3:8b` output with `think: false` enabled.
- T015D-rerun result: `PHASE8-IMPL-023-T015D-rerun - Rerun manual real Ollama qwen3:8b validation after T015E` is complete/PASS-SAFE-FAIL-CLOSED. T015D was rerun after Windows Ollama repair (0.31.2) and T015E `think: false` fix. Ollama API reachable (version 0.31.2); `qwen3:8b` confirmed available with `completion`, `tools`, `thinking` capabilities. T015E payload confirmed via direct `/api/chat` smoke test (trivial "ping" 3.7s; full T006 system prompt 111.3s, valid JSON in `message.content` with `done_reason: "stop"`). Live adapter correctly reached behind env flags but fail-closed at finite `urllib.request.urlopen(timeout=30)` boundary because `qwen3:8b` with full T006 system prompt takes ~111s on this hardware. Adapter correctly failed closed with no findings, no persistence, no Memory/Canon mutation, no promotion records, no apply-promotion, no story prose. `persist_candidates=False` honored. All 87 automated tests passed. Prose guard blocked task's suggested 35-word narrative text (same as original T015D); shorter non-prose input reached live adapter. No backend/frontend/package/dependency files changed during T015D rerun. Timeout tuning and prose guard review are separate future concerns. T015D original was PASS-SAFE-FAIL-CLOSED (missing `llama-server.exe`); T015D rerun is PASS-SAFE-FAIL-CLOSED (finite adapter timeout < `qwen3:8b` inference time); T015E `think: false` fix confirmed working end-to-end via direct API test.
- T004 deterministic marker extraction status: `PHASE8-IMPL-023-T004` remains historically complete/PASS, but is superseded and re-scoped as fallback/safety baseline only. It is not sufficient for MVP completion by itself.
- T014D result: `PHASE8-IMPL-023-T014D - Live spaCy manual validation` is complete/PASS (rerun after owner-installed spaCy). Decision record: `docs/roadmap/decisions/PHASE8-IMPL-023-T014D-manual-local-spacy-validation.md`. T014D was rerun after the owner installed spaCy and `en_core_web_sm` in `.venv-unsloth-clean`. spaCy package and model loaded successfully. The live adapter was manually run with `OMI_LIVE_TOOLS_ENABLED=1 OMI_LIVE_SPACY_ENABLED=1 OMI_LIVE_SPACY_MODEL=en_core_web_sm` and `persist_candidates=False`. The live spaCy adapter returned `analysis_status: succeeded` with 4 evidence-backed candidate-only findings: characters (Mara Vale, Jonah Cross), location (Vancouver), object (a brass compass). All findings have source excerpts, source locators, provenance, support labels (not truth), pending owner decisions, candidate fingerprints, evidence fingerprints, and normalized finding IDs. No candidate persistence, Memory/Canon mutation, promotion records, apply-promotion, or story prose occurred. Automated tests: 16 passed, 1 expected failure confirming spaCy is installed. T014D is now complete/PASS with real-runtime spaCy validation evidence.

- T014E result: `PHASE8-IMPL-023-T014E - Live runtime tool installation inventory` is complete/PASS. Decision record: `docs/roadmap/decisions/PHASE8-IMPL-023-T014E-live-runtime-tool-installation-inventory.md`. All seven selected PHASE8-IMPL-023 tools inventoried. spaCy: NOT installed (adapter exists, blocked). Ollama: NOT installed in WSL (but installed on Windows per owner confirmation; T015A verified WSL reachability). Story Check: in-repo module (no live adapter). BookNLP: NOT installed (reference source exists). NCP: protocol/schema (needs owner decision). Subtxt: reference docs (needs owner decision). dramatica-flow: reference-only per PHASE8-IMPL-005 (needs owner decision). No installs/downloads performed. No backend/frontend/package files changed. Owner must install spaCy, and decide on NCP/Subtxt/dramatica-flow before T015-T020 can proceed.
- T015B result: `PHASE8-IMPL-023-T015B - Ollama HTTP preflight and model availability` is complete/PASS. Decision record: `docs/roadmap/decisions/PHASE8-IMPL-023-T015B-ollama-http-preflight-and-model-availability.md`. T015B adds read-only Ollama HTTP API reachability and model availability probing to the T013 preflight using `urllib.request` only. Probes `/api/version` and `/api/tags`. Env vars: `OMI_LIVE_OLLAMA_BASE_URL` (default `http://127.0.0.1:11434`), `OMI_LIVE_OLLAMA_MODEL` (default `qwen3:8b`). Reports `ollama_base_url`, `ollama_api_available`, `ollama_version`, `ollama_model_name`, `ollama_model_available`, `ollama_probe_detail`. Fail-closed on connection errors, timeouts, invalid JSON, missing fields, unavailable model. WSL gateway IP not hardcoded; configurable via env var. Supports Windows-hosted Ollama. 9 new tests (all monkeypatched). No live adapter, no model generation, no candidates, no frontend/package changes.
- T015A result: `PHASE8-IMPL-023-T015A - Ollama runtime and fixture contract inspection` is complete/PASS. Decision record: `docs/roadmap/decisions/PHASE8-IMPL-023-T015A-ollama-runtime-and-fixture-contract-inspection.md`. Ollama CLI is NOT available in WSL; Windows Ollama service IS reachable from WSL at the WSL gateway IP (`172.25.144.1:11434`) but NOT at `127.0.0.1:11434`. Ollama version 0.31.1. Installed models: `nomic-embed-text:latest`, `qwen3:8b`, `qwen2.5:7b`, `phi3:mini`. Selected MVP model candidate: `qwen3:8b`. The existing fixture contract (`omi_ollama_structured_extraction.v1`) and T013 preflight flags were mapped. No live adapter, model analysis, candidates, or backend/frontend/package changes occurred.
- T014C result: `PHASE8-IMPL-023-T014C - Live spaCy adapter behind flags` is complete/PASS. Decision record: `docs/roadmap/decisions/PHASE8-IMPL-023-T014C-live-spacy-adapter-behind-flags.md`. T014C adds `_build_spacy_live_runner` to `backend/omi_analysis_orchestrator.py`, which imports spaCy lazily, loads the configured model (default `en_core_web_sm`, overridable via `OMI_LIVE_SPACY_MODEL`), extracts entities and non-overlapping noun chunks into candidate-only normalized findings, and fail-closes on ImportError/OSError/runtime exceptions. The live path is gated behind `OMI_LIVE_TOOLS_ENABLED` + `OMI_LIVE_SPACY_ENABLED` env flags and is disabled by default. `tests/test_omi_booknlp_spacy_adapter_contract.py` reports `17 passed` (10 existing fixture + 7 new T014C). All tests mock spaCy and do not require real spaCy. No spaCy package/model install/download was performed. Fixture/mock spaCy contract remains supported and remains scaffolding only. T014D (manual validation with real spaCy) is now complete/PASS-WITH-BLOCKED-RUNTIME.
- T014A result: `PHASE8-IMPL-023-T014A - spaCy fixture contract and T013 preflight inspection` is complete/PASS as inspect/docs only. Decision record: `docs/roadmap/decisions/PHASE8-IMPL-023-T014A-spacy-fixture-contract-and-preflight-inspection.md`. T014A mapped the existing spaCy fixture/mock contract (`omi_spacy_local_nlp_extraction.v1`), the existing T013 preflight/feature-flag/health-check support, and identified the exact next slice for T014B (runtime availability check) and T014C (live adapter implementation). No live spaCy runtime was connected; no dependencies were installed; no backend/frontend runtime behavior was edited. Fixture/mock spaCy contract remains scaffolding only, not MVP completion.
- T014B result: `PHASE8-IMPL-023-T014B - spaCy runtime availability check` is complete/PASS as read-only spaCy package and model availability probe in the T013 preflight. Decision record: `docs/roadmap/decisions/PHASE8-IMPL-023-T014B-spacy-runtime-availability-check.md`. T014B adds `_spacy_model_probe` reporting package availability, model availability, and model name (default `en_core_web_sm`, overridable via `OMI_LIVE_SPACY_MODEL`) with fail-closed read-only behavior. Tests report `14 passed` (8 existing + 6 new). No live spaCy adapter was implemented; no analysis was run; no candidates were produced; no install/download occurred. Fixture/mock spaCy contract remains scaffolding only, not MVP completion.
- T005 result: `PHASE8-IMPL-023-T005 - Tool-assisted extraction orchestrator contract and adapter boundaries` is complete/PASS as orchestrator contract and adapter-boundary scaffold only (no real AI/tool runtime calls; deterministic_fallback wired to the existing T004 marker extractor as fallback/safety baseline only). Decision record: `docs/roadmap/decisions/PHASE8-IMPL-023-T005-tool-assisted-orchestrator-contract-adapter-boundaries.md`. Implementation: `backend/omi_analysis_orchestrator.py` exports `analyze_omi_raw_idea_with_tools` plus adapter identity and result-state constants, normalized finding schema, no-prose guard, fail-closed behavior, fusion/dedupe contract fields, and deterministic fingerprints. Tests: `tests/test_omi_tool_assisted_orchestrator_contract.py` -> `26 passed`.
- T006 result: `PHASE8-IMPL-023-T006 - Ollama/model-assisted structured extraction contract with JSON/schema validation and no-prose tests` is complete/PASS as fixture-only Ollama/model adapter support. Decision record: `docs/roadmap/decisions/PHASE8-IMPL-023-T006-ollama-model-structured-extraction-contract.md`. Implementation: `backend/omi_analysis_orchestrator.py` validates `omi_ollama_structured_extraction.v1` JSON/dict fixtures, normalizes successful `ollama_model` findings into the T005 normalized finding schema, rejects invalid/unsafe/no-prose/truth/auto-approval output fail-closed with no findings, keeps `requested_adapters=["ollama_model"]` without fixtures unavailable with no live call, and keeps Ollama findings non-persistent even when `persist_candidates=True`. Tests: `tests/test_omi_ollama_model_adapter_contract.py` -> `19 passed`.
- T007 result: `PHASE8-IMPL-023-T007 - BookNLP/spaCy local NLP candidate extraction adapters` is complete/PASS as fixture-only local NLP adapter support. Decision record: `docs/roadmap/decisions/PHASE8-IMPL-023-T007-booknlp-spacy-local-nlp-candidate-extraction-adapters.md`. Implementation: `backend/omi_analysis_orchestrator.py` validates `omi_booknlp_local_nlp_extraction.v1` and `omi_spacy_local_nlp_extraction.v1` JSON/dict fixtures, normalizes successful `booknlp` and `spacy` findings into the T005 normalized finding schema, rejects invalid/missing evidence/source locator/provenance/no-prose/truth/auto-approval/Memory-Canon-mutation/promotion/apply-promotion output fail-closed with no findings, keeps `requested_adapters=["booknlp"]` and `["spacy"]` without fixtures unavailable with no live runtime call, and keeps BookNLP/spaCy findings non-persistent even when `persist_candidates=True`. Tests: `tests/test_omi_booknlp_spacy_adapter_contract.py` -> `10 passed`.
- T008 result: `PHASE8-IMPL-023-T008 - Story Check diagnostic-only OMI handoff` is complete/PASS as fixture-only Story Check diagnostic adapter support. Decision record: `docs/roadmap/decisions/PHASE8-IMPL-023-T008-story-check-diagnostic-only-omi-handoff.md`. Implementation: `backend/omi_analysis_orchestrator.py` validates `omi_story_check_diagnostic_handoff.v1` JSON/dict fixtures, normalizes successful `story_check` structural diagnostics, storyform/throughline context support, plot-thread/relationship diagnostics, and diagnostic questions into the T005 normalized finding schema, rejects invalid JSON/schema/status/adapter/finding type/evidence/source locator/provenance/no-prose/truth/final/auto-approval/review-approved/Memory-Canon-mutation/candidate-persistence/promotion/apply-promotion output fail-closed with no findings, keeps `requested_adapters=["story_check"]` without fixtures unavailable with no live Story Check call, and keeps Story Check findings non-persistent even when `persist_candidates=True`. Tests: `tests/test_omi_story_check_adapter_contract.py` -> `11 passed`.
- T009 result: `PHASE8-IMPL-023-T009 - NCP/Subtxt/dramatica-flow diagnostic and context adapter contracts` is complete/PASS as fixture-only NCP/Subtxt/dramatica-flow diagnostic/context adapter support. Decision record: `docs/roadmap/decisions/PHASE8-IMPL-023-T009-ncp-subtxt-dramatica-flow-diagnostic-context-adapters.md`. Implementation: `backend/omi_analysis_orchestrator.py` validates `omi_ncp_context_handoff.v1`, `omi_subtxt_diagnostic_handoff.v1`, and `omi_dramatica_flow_analysis_handoff.v1` JSON/dict fixtures, normalizes successful `ncp`, `subtxt`, and `dramatica_flow` context/diagnostic findings and diagnostic questions into the T005 normalized finding schema, rejects invalid JSON/schema/status/adapter/finding type/evidence/source locator/provenance/no-prose/truth/final/auto-approval/review-approved/Memory-Canon-mutation/candidate-persistence/promotion/apply-promotion output fail-closed with no findings, keeps each adapter without fixtures unavailable with no live runtime call, and keeps all three adapters' findings non-persistent even when `persist_candidates=True`. Tests: `tests/test_omi_ncp_subtxt_dramatica_flow_adapter_contract.py` -> `31 passed`.
- T010 result: `PHASE8-IMPL-023-T010 - Candidate fusion, dedupe, conflict handling, and evidence/provenance normalization` is complete/PASS as backend/orchestrator-contract fusion. Decision record: `docs/roadmap/decisions/PHASE8-IMPL-023-T010-fusion-dedupe-conflict-uncertainty-contract.md`. Implementation: `backend/omi_analysis_orchestrator.py` now annotates normalized fixture-only adapter findings with deterministic candidate/evidence/normalized-finding fingerprints, duplicate links, related finding IDs, deterministic conflict groups, support-only uncertainty labels, preserved evidence/provenance/source locator/owner decision/review status/support metadata, and a compact `fusion_summary`. The pass preserves every finding, never decides truth, never merges conflicts into canon, never persists AI/tool findings, and does not mutate Memory/Canon. Tests: `tests/test_omi_tool_assisted_fusion_dedupe_contract.py` -> `5 passed`.
- T011 result: `PHASE8-IMPL-023-T011 - Candidate-only persistence for fused AI/tool-assisted findings` is complete/PASS as backend/orchestrator candidate-only persistence. Decision record: `docs/roadmap/decisions/PHASE8-IMPL-023-T011-candidate-only-persistence-for-fused-ai-tool-findings.md`. Implementation: `backend/project_manager.py` now validates and persists fused AI/tool findings through existing OMI candidate queue records only when `persist_candidates=True`, a source OMI idea exists, and the raw idea snapshot matches; `backend/omi_analysis_orchestrator.py` reports persistence status/explanation plus new/reused candidate IDs. Persisted records preserve evidence, source locators, provenance, source adapter identity, support-only metadata, fingerprints, normalized finding IDs, duplicate relations, conflict groups, and uncertainty labels, with owner decision pending and review status candidate/review-pending. Reruns reuse matching source/finding candidate IDs. Tests: `tests/test_omi_tool_assisted_persistence_contract.py` -> `5 passed`.
- T013 result: `PHASE8-IMPL-023-T013 - Runtime configuration, preflight, health checks, and feature flags` is complete/PASS as read-only runtime preflight foundation. Decision record: `docs/roadmap/decisions/PHASE8-IMPL-023-T013-runtime-preflight-health-checks-and-feature-flags.md`. Implementation: `backend/omi_runtime_preflight.py` reports each selected tool's fixture contract existence, runtime configuration, dependency availability, feature-flag enablement, blocked state, and safe status vocabulary (`enabled`, `disabled`, `available`, `unavailable`, `blocked`, `error`, `not_configured`); `backend/main.py` exposes read-only `GET /api/projects/{project_name}/omi/runtime-preflight`. Tests: `tests/test_omi_live_runtime_preflight_contract.py` -> `8 passed`.
- Required MVP behavior: user enters or saves raw idea text in OMI; OMI runs real local/runtime tool-assisted analysis through all selected tools unless a tool is explicitly documented as BLOCKED by owner decision; adapters return candidate findings, diagnostics, evidence, provenance, source locators, and support labels; normalization converts outputs to a common OMI candidate schema; fusion/dedupe groups equivalent findings and preserves conflicts/uncertainty; candidate-only persistence is validated against real runtime outputs; the UI lists real runtime findings grouped by type/tool/evidence for owner review.
- Required candidate types where evidence exists: characters, locations, timeline/events, relationships, organizations/groups, objects/items, plot threads or story facts, open questions/ambiguities, storyform/context candidates only when supportable, and diagnostics/questions where appropriate.
- Acceptance criteria include non-empty tool-assisted candidate findings where evidence exists, visible grouped candidate list, candidate type/label/claim/evidence/source locator/provenance/tool source/support/status/owner decision state, fail-closed empty or unsupported analysis, no misleading empty shells, no automatic Memory/Canon mutation, no automatic apply-promotion, no generated story prose, and no tool/model output as truth.
- UI/UX improvement scope includes raw idea intake state, analysis status/progress/result, candidate grouping by type, tool/provenance grouping, evidence/source locator display, conflict/uncertainty display, empty/fail-closed states, review queue clarity, better error messages, and clear next action after candidate analysis.
- Full MVP completion closeout remains blocked until `PHASE8-IMPL-023` or an equivalent MVP-required AI/tool-assisted OMI analysis path is complete and validated with real local/runtime OMI and analysis outputs from every selected tool, or explicit owner-blocked decisions for unavailable tools, plus grouped owner-review UI and automated/manual live validation.
- Current owner gate status: explicit owner Accepted/Blocked gate decision is complete/PASS with result ACCEPTED. `PHASE8-UX-003` remains complete/PARTIAL historically as the owner acceptance harness route/workflow evidence follow-up, but its owner gate is now ACCEPTED by explicit owner decision. `PHASE8-UX-004` remains complete/PASS for OMI manual workflow repair only, and its owner manual test remains APPROVED.
- Accepted manual limitations remain visible: `PHASE8-UX-003-C-CYBER` remains `MANUAL_REVIEW_REQUIRED`; A-category blockers were manual owner review only; PHASE8-UX-004 repaired owner-test confusion only; OMI manual workflow approval did not implement extraction; approval remains lifecycle/status metadata only; approval does not mutate Memory/Canon; promotion remains separate/guarded.
- Immediate completed owner-test repair task: `PHASE8-UX-004` - OMI Manual Workflow Repair and Extraction Availability Labeling (complete/PASS for confusion repair only).
- Immediate completed parent task: `PHASE8-IMPL-022` - End-to-end MVP usability validation.
- Last completed parent task: `PHASE8-IMPL-022` - End-to-end MVP usability validation (complete/PASS through `PHASE8-IMPL-022-T007`).
- Active/last owner-gate parent task: `PHASE8-UX-003` - Owner Acceptance Harness Route/Workflow Evidence Follow-up (complete/PARTIAL historically; no active child; owner gate ACCEPTED by explicit owner decision).
- Historical owner-gate parent records: `docs/roadmap/tasks/PHASE8-UX-003.md`, `docs/roadmap/inventory/PHASE8-UX-003.md`, `docs/roadmap/enrichment/PHASE8-UX-003.enrichment.json`.
- Last completed child task: `PHASE8-UX-003-T007` - Closeout and owner Accepted/Blocked gate preparation (complete/PARTIAL).
- Active child task: `PHASE8-IMPL-023-T014`.
- Latest PHASE8-IMPL-023 decision: `docs/roadmap/decisions/PHASE8-IMPL-023-T013-runtime-preflight-health-checks-and-feature-flags.md` records the read-only runtime preflight foundation for selected live OMI/analysis tools. T013 adds disabled-by-default feature flags, safe dependency/config probes, explicit blocked-state support, and read-only route reporting without running live analysis, external services, live models, candidate persistence, Memory/Canon mutation, promotion/apply-promotion, or story prose generation. `docs/roadmap/decisions/PHASE8-IMPL-023-T012A-real-local-runtime-tools-required-for-omi-mvp.md` remains controlling closeout truth: fixture/mock adapter contracts and preflight readiness do not prove live analysis and do not count as MVP completion. T014 onward must connect and validate real runtime tools before T023 grouped owner-review UI and T026 parent closeout.
- Latest PHASE8-UX-004 owner manual-test approval: `docs/roadmap/decisions/PHASE8-UX-004-owner-manual-test-approval.md` records owner result APPROVED for the OMI manual workflow repair only. The approved expected behavior is no extraction yet; the UI clearly labels empty/manual candidate shells; the UI clearly states automatic extraction is not available from the OMI manual screen yet; approval changes lifecycle/status metadata only; approval does not mutate Memory/Canon; and promotion remains separate/guarded. That approval did not itself mark final MVP owner acceptance PASS or MVP complete; the separate final owner gate is now ACCEPTED by explicit owner decision in `docs/roadmap/decisions/PHASE8-final-owner-accepted-gate-decision.md`.
- Latest PHASE8-UX-004 decision: `docs/roadmap/decisions/PHASE8-UX-004-omi-manual-workflow-repair-decision.md` records the manual OMI workflow repair as complete/PASS for owner-test confusion only. The UI now labels raw idea capture as owner-authored planning input, labels empty OMI candidates as manual shells, states automatic extraction of characters/locations/timeline/story facts is not available from this screen yet, maps decisions to backend-valid statuses, requires approval confirmation, surfaces backend validation errors, keeps approval lifecycle-only, and keeps promotion separate/guarded. Automatic extraction remains unavailable from the OMI manual screen unless separately implemented later. Owner acceptance is not PASS and MVP is not complete.
- Latest PHASE8-UX-003 owner harness route evidence: `tests/test_phase8_ux003_owner_harness_expected_red.py` -> `6 passed in 0.04s`; `tests/test_mvp_owner_acceptance_browser_smoke_source.py` -> `24 passed in 0.11s`; `node scripts/mvp-owner-acceptance-browser-smoke.mjs` -> exit `0`, final automated decision `MANUAL_REVIEW_REQUIRED`. `PHASE8-UX-003-B` remains PASS and wired to existing OMI Dashboard, Candidate Detail, Evidence Drawer, and Apply-Promotion Confirmation PASS evidence. `PHASE8-UX-003-C-NON-CYBER` remains PASS and wired to existing Notes/Materials save/reload proof plus analysis-runtime label/status evidence. T006 confirms the Cyber fixture can be imported/selected through existing owner-authored source UI and confirms existing no-prose refusal/fail-closed UI covers rewrite, continue, outline, draft, polish, improve, expand, imitate, and generate prose without submitting unsafe prompts. `PHASE8-UX-003-C-CYBER` remains `MANUAL_REVIEW_REQUIRED` because selected-source Story Check reached the safe route but model-backed diagnostic output was unavailable/fail-closed on missing `storyform.json`; no diagnostic model output was claimed.
- Latest PHASE8-UX-003 decision: `docs/roadmap/decisions/PHASE8-UX-003-T007-closeout-owner-gate-preparation.md` closes `PHASE8-UX-003-T007` as complete/PARTIAL and closes the parent as complete/PARTIAL. `PHASE8-UX-003-B` is PASS, `PHASE8-UX-003-C-NON-CYBER` is PASS, and `PHASE8-UX-003-C-CYBER` remains `MANUAL_REVIEW_REQUIRED`; A-category blockers were manual owner review only. The later final owner gate is ACCEPTED by explicit owner decision, without converting the Cyber manual-review item into automated PASS.
- Latest owner/roadmap gate decision: `docs/roadmap/decisions/PHASE8-UX-002-T007-owner-acceptance-gate-decision.md` classifies remaining owner acceptance blockers as owner manual review only, OMI evidence already PASS but not wired into the owner harness, or owner harness route/workflow follow-up. It does not mark owner acceptance PASS and does not mark MVP complete.
- Planned child sequence: T001 historical gap audit/architecture decision (complete/PASS, partially superseded); T002 historical expected-red tests (complete/PASS); T003 historical backend extraction contract/schema (complete/PASS); T004 historical deterministic/rule-based backend extractor (complete/PASS, fallback-only); T004A owner override and AI/tool-assisted OMI architecture reset (complete/PASS); T005 tool-assisted extraction orchestrator contract and adapter boundaries (complete/PASS, contract/scaffold only); T006 Ollama/model-assisted structured extraction contract with JSON/schema validation and no-prose tests (complete/PASS, fixture-only); T007 BookNLP/spaCy local NLP candidate extraction adapters (complete/PASS, fixture-only); T008 Story Check diagnostic-only OMI handoff (complete/PASS, fixture-only); T009 NCP/Subtxt/dramatica-flow diagnostic and context adapter contracts (complete/PASS, fixture-only); T010 candidate fusion/dedupe/conflict handling/evidence-provenance normalization (complete/PASS, backend-only); T011 candidate-only persistence for fused AI/tool-assisted findings (complete/PASS, backend-only); T012A real local/runtime tools required for OMI MVP roadmap reset (complete/PASS, docs/status reset); T013 runtime configuration, preflight, health checks, and feature flags (complete/PASS, read-only preflight foundation); T014 live spaCy integration (complete/PASS through T014E); T015 live Ollama/local model integration (T015A inspect complete/PASS, T015B preflight HTTP probe complete/PASS, T015C live adapter implementation complete/PASS, T015D manual validation complete/PASS-SAFE-FAIL-CLOSED, T015D-rerun complete/PASS-SAFE-FAIL-CLOSED, T015D-rerun-after-T015F complete/PASS, T015E disable Qwen thinking mode complete/PASS, T015F narrow no-prose guard scope and configure Ollama timeout complete/PASS); T016A Story Check runtime surface and fixture contract inspection (complete/PASS, inspect/docs only); T016B Story Check runtime preflight/config/availability check (complete/PASS, recommended next after T016A); T016C live Story Check adapter behind flags (complete/PASS, recommended next after T016B); T016C1 tighten Story Check live converter sanitizer boundary (complete/PASS, recommended next after T016C); T016D manual real Story Check validation (complete/PASS, recommended next after T016C1); T017A BookNLP runtime preflight after install (complete/PASS, recommended next after T016D); T017B live BookNLP adapter behind flags (complete/PASS, recommended next after T017A); T017C manual real BookNLP validation (complete/PASS-SAFE-FAIL-CLOSED, recommended next after T017B); T017C1 narrow repair of `transformers` vs BookNLP model state-dict `position_ids` compatibility and T017C rerun (planned, recommended next after T017C); T017 live BookNLP integration in OMI and analysis (planned, parent); T018A NCP schema validator preflight (complete/PASS, recommended next after T017C1 if T017C1 reaches a real candidate-only findings stream; the T018A surface is now reported by the read-only preflight and the T018B candidate-import validation adapter is the recommended next task); T018B NCP candidate-import validation adapter (complete/PASS, mocked automated tests + preflight/docs/status; the T018B live adapter is gated behind `OMI_LIVE_TOOLS_ENABLED=1` + `OMI_LIVE_NCP_ENABLED=1` + not `OMI_LIVE_NCP_BLOCKED` + an explicit `OMI_LIVE_NCP_INPUT_PATH` and an opt-in `OMI_LIVE_NCP_VALIDATE_WITH_NODE=1` whose subprocess step is mocked in tests; manual real NCP validation is intentionally deferred to T018C); T018C manual real NCP validation against an owner-selected real NCP JSON file (complete/PASS-SAFE-FAIL-CLOSED, recommended next after T018B; T018C confirmed the live NCP adapter is reachable through the OMI orchestrator behind the live flags and the fail-closed path is correct and safe, but the T018B `_ncp_resolve_allowed_input_path` overreaching path-parts walk rejects the selected NCP file because this workspace parent directory is literally named `projects`; the live runner did not emit findings and the orchestrator returned `analysis_status: "fail_closed"` with full safety envelope preserved; the bug is in the allowlist granularity, not in the safety contract); T018C1 narrow repair of `_ncp_resolve_allowed_input_path` overreaching `for part in resolved.parts: if part == "projects"` segment walk (complete/PASS, repaired the allowlist overreach with a strict `_ncp_path_is_relative_to` containment check; preserved the T018B traversal / symlink / directory / not-a-file / `tests_root` / `temp_root` / `external_root` allowlist contract; 10 new T018C1 regression tests + 1 minor rewrite of the existing T018B `test_live_ncp_path_allowlist_rejects_projects_tree` test; T018C-rerun was the recommended next task to confirm real candidate-only findings); T018C-rerun manual real NCP validation rerun after T018C1 allowlist repair (complete/PASS, ran the committed T018B live NCP candidate-import validation adapter through `analyze_omi_raw_idea_with_tools` against the same owner-selected real NCP JSON file T018C used; the T018C1-repaired allowlist now accepts the selected `.external_sources/...` path; `analysis_status: "succeeded"`, `findings: 64`, all `candidate_type: "story_fact"`, all carrying the required evidence/provenance/source-locator / pending-owner / candidate-review-pending fields, full result-level safety envelope preserved, no project / `.external_sources` / `ai_context` / `graphify-out` / `artifacts/mvp-readiness/owner-acceptance/` mutation, `git diff --check` clean, all post-T018C1 automated regressions still pass); T018 live NCP integration in OMI and analysis (planned, parent); T019A Subtxt docs/source preflight (complete/PASS, recommended next after T018C-rerun; the T019A preflight mirrored the T018A NCP preflight for the Subtxt surface); T019B Subtxt integration-path decision (complete/PASS, `Live Subtxt runtime: OWNER-BLOCKED`, `App-owned Subtxt-informed semantic-rubric path: ACCEPTED`); T019C app-owned Subtxt-informed semantic-rubric contract (complete/PASS, recommended next after T019B; the pure app-owned input/output contract with the new `app_owned_subtxt_informed_rubric` identity and the new `App-owned Subtxt-informed diagnostic support` support label; 91 focused in-memory tests in `tests/test_omi_subtxt_informed_semantic_rubric_contract.py` pass); T019D app-owned Subtxt-informed semantic-rubric evaluator (complete/PASS, recommended next after T019C; the pure local deterministic/rule-assisted in-memory evaluator that consumes the T019C request contract and produces the T019C result contract; 81 focused in-memory tests in `tests/test_omi_subtxt_informed_semantic_rubric_evaluator.py` pass); T019E app-owned Subtxt-informed semantic-rubric OMI adapter integration (complete/PASS, recommended next after T019D; integrates the committed T019D evaluator into the OMI analysis orchestrator under a new app-owned adapter identity `subtxt_informed_rubric`, distinct from the existing T009 `subtxt` adapter identity, distinct from the T019C/T019D internal rubric identity `app_owned_subtxt_informed_rubric`, and distinct from the user-facing support label `App-owned Subtxt-informed diagnostic support`; 78 focused in-memory tests in `tests/test_omi_subtxt_informed_semantic_rubric_omi_adapter_contract.py` pass; the explicit `requested_adapters=["subtxt_informed_rubric"]` request is the only activation path); T019F manual real owner-authored Subtxt-informed rubric validation (complete/PASS, recommended next after T019E; ran one explicit real in-process OMI orchestrator validation through `analyze_omi_raw_idea_with_tools(requested_adapters=["subtxt_informed_rubric"], persist_candidates=False, project_name="example", source_idea_id="t019f_owner_validation")` against the controlled non-story owner-authored raw idea; the committed T019E built-in adapter and the committed T019D evaluator were each reached exactly once; `analysis_status="succeeded"`, `persistence_status="not_requested"`, `findings: 8` evidence-backed candidate-only findings; expected category coverage present; provenance/owner-decision/safety-envelope assertions passed; before/after protected snapshots identical; no live Subtxt execution or claim; live Subtxt runtime remains owner-blocked; the accepted app-owned Subtxt-informed path is now both implemented and manually validated through OMI; the validated app-owned adapter is clearly distinguished from the existing T009 `subtxt` fixture identity); T019 live Subtxt integration in OMI and analysis (complete/PASS, parent; T019A + T019B + T019C + T019D + T019E + T019F are all complete/PASS; live Subtxt runtime remains owner-blocked; the accepted app-owned Subtxt-informed semantic-rubric path is implemented and manually validated through OMI; next parent task is T020); T020 live dramatica-flow integration in OMI and analysis (planned, recommended next after T019; do not skip to T021, T022, T023, T024, T025, or T026); T021 cross-tool fusion validation using real runtime outputs (planned); T022 candidate-only persistence validation using real runtime outputs (planned); T023 grouped owner-review UI for real runtime findings (planned); T024 automated end-to-end live OMI test (planned); T025 manual Cyber Detective Story live OMI test (planned); T026 parent closeout only after live runtime tools are connected/tested or explicitly owner-blocked (planned).
- Latest validation evidence: PHASE8-IMPL-023-T013 runtime preflight is recorded in `docs/roadmap/decisions/PHASE8-IMPL-023-T013-runtime-preflight-health-checks-and-feature-flags.md`. It added `backend/omi_runtime_preflight.py`, read-only `GET /api/projects/{project_name}/omi/runtime-preflight` in `backend/main.py`, and `tests/test_omi_live_runtime_preflight_contract.py` (`8 passed`). It did not run frontend UI, browser harness scripts, live NCP/Subtxt/dramatica-flow, live Story Check, live model/Ollama, live BookNLP/spaCy, Memory/Canon mutation, candidate persistence, apply-promotion run/enablement, promotion record creation, dependency installation, or story prose. The prior frontend/source expected-red is deferred to T023 after live runtime integration.
- Current parent note: `PHASE8-UX-003` is complete/PARTIAL as the separate owner acceptance harness route/workflow evidence follow-up after `PHASE8-UX-002-T007`. T001 created the parent record, inventory, enrichment JSON, and roadmap/status updates only. T002 accepted the docs-only route/workflow mapping decision. T003 is complete/PASS as tests-first expected-red coverage only. T004 is complete/PASS for B and non-Cyber C route wiring. T005 is complete/PARTIAL for Cyber fixture routing. T006 is complete/PASS for harness rerun and remaining manual item classification. T007 is complete/PARTIAL for closeout and owner gate preparation: B PASS, non-Cyber C PASS, Cyber C `MANUAL_REVIEW_REQUIRED`, A-category blockers manual owner review only, and no follow-up implementation needed before owner gate review. `PHASE8-UX-002` is not reopened. No new product UI is authorized by default. Owner acceptance is now PASS by explicit owner decision, while full MVP completion remains distinct if separately required by roadmap convention. Generated prose/prose-production remains permanently forbidden, and apply-promotion remains guarded/disabled in browser smoke evidence unless separately authorized.

## MVP Scope Revision

`PHASE8-MVP-SCOPE-REVISION-001` revises the MVP definition. The MVP is not considered usable/testable until the roadmap delivers runtime extraction, project-local raw artifact persistence, candidate review, frontend owner-action execution, explicit audited apply-promotion, approved memory/canon mutation through owner-approved workflow, model-assisted evidence-backed extraction, analysis-only NCP/Subtxt/dramatica-flow runtime integration, and real BookNLP/spaCy install/run/import with environment guards and install/run/import tests. `PHASE8-IMPL-014` is complete through `PHASE8-IMPL-014-T007`. `PHASE8-IMPL-015` is complete through `PHASE8-IMPL-015-T007`. `PHASE8-IMPL-016` is complete through `PHASE8-IMPL-016-T007`. `PHASE8-IMPL-017` is complete through `PHASE8-IMPL-017-T007`. `PHASE8-IMPL-018` is complete through `PHASE8-IMPL-018-T007`. `PHASE8-IMPL-019` is complete through `PHASE8-IMPL-019-T007`. `PHASE8-IMPL-020` is complete/PASS through `PHASE8-IMPL-020-T007`. `PHASE8-IMPL-021` is complete/PASS through `PHASE8-IMPL-021-T007`. `PHASE8-IMPL-022` is complete/PASS through `PHASE8-IMPL-022-T007` as the end-to-end MVP usability validation parent. `PHASE8-UX-002` is complete/PARTIAL through T007 with final automated decision `MANUAL_REVIEW_REQUIRED`. `PHASE8-UX-003` is complete/PARTIAL through T007 as the owner acceptance harness route/workflow evidence follow-up. The MVP owner gate is now ACCEPTED by explicit owner decision, and owner acceptance is PASS by explicit owner decision, while full MVP completion remains distinct if separately required by roadmap convention. Fine-tuning remains the only major listed feature deferred after MVP. Generated prose, rewrite, continuation, imitation, polish, improvement, expansion, outline generation, and prose-production paths remain permanently forbidden and are not future roadmap features.
- Prior parent note: `PHASE8-IMPL-012` is complete through `PHASE8-IMPL-012-T007`. It was a planning/contract parent that decided whether and how the in-memory review queue entries produced by the completed `PHASE8-IMPL-011` candidate review gate should be stored, listed, loaded, and prepared for future owner-review workflows without turning queue state into approval, canon truth, apply-promotion, review UI/API scope creep, or memory/canon mutation. It delivered review queue storage validation, a project-local queue storage helper, a derived index, and owner action record validation only. No review UI/API, backend routes, frontend UI, apply-promotion, memory/canon mutation, or runtime extraction exists yet. `PHASE8-IMPL-012-T001` published the parent task record, inventory, and enrichment JSON as docs/status/planning only. `PHASE8-IMPL-012-T002` accepted the review queue storage contract decision at `docs/roadmap/decisions/PHASE8-IMPL-012-review-queue-storage-contract-decision.md` as docs/decision only: a project-local, candidate-linked, candidate-only, review-workflow-only, evidence/provenance-backed, fail-closed review queue storage contract with a future storage root `projects/{project_id}/writer_assistant/review_queue/`, a stored queue entry shape and allowed `review_status`/`lifecycle_state` values aligned with the `PHASE8-IMPL-011-T003` queue shape, candidate linkage/integrity rules, a derived/rebuildable index contract, a storage operation boundary, and forbidden destinations. `PHASE8-IMPL-012-T003` accepted the owner action workflow boundary decision at `docs/roadmap/decisions/PHASE8-IMPL-012-owner-action-workflow-boundary-decision.md` as docs/decision only: owner actions are review workflow commands and states only, not apply-promotion, not memory/canon mutation, and not approval/canon truth; the decision records allowed owner action commands and states, a future owner action record shape, the queue entry mutation boundary, the candidate record relationship, apply-promotion and memory/canon boundaries, evidence/provenance requirements, an owner action storage boundary, a deferred review UI/API boundary, and a fail-closed/quarantine policy. `PHASE8-IMPL-012-T004` added expected-red review queue storage contract tests at `tests/test_writer_assistant_core_review_queue_storage_contract.py` for the future `backend.story_knowledge.review_queue_storage` module and its `validate_review_queue_entry`, `build_review_queue_entry_from_candidate_record`, `review_queue_storage_dir`, `review_queue_entry_path`, `review_queue_index_path`, `write_review_queue_entry`, `read_review_queue_entry`, `list_review_queue_entries`, `build_review_queue_index`, and `validate_owner_action_record` APIs; the target pytest is expected-red with a collection `ImportError` limited to the missing future module, and candidate review gate/candidate/orchestrator/source-evidence regressions (632) and focused OMI/project regressions (109) pass. `PHASE8-IMPL-012-T005` created `backend/story_knowledge/review_queue_storage.py`, a pure, standard-library-only, deterministic, project-local, candidate-linked, candidate-only, review-workflow-only queue storage helper over existing candidate validation and candidate review gate helpers, implementing `validate_review_queue_entry`, `build_review_queue_entry_from_candidate_record`, `review_queue_storage_dir`, `review_queue_entry_path`, `review_queue_index_path`, `write_review_queue_entry`, `read_review_queue_entry`, `list_review_queue_entries`, `build_review_queue_index`, and `validate_owner_action_record`; the target review queue storage contract now passes (359 tests), candidate review gate/candidate/orchestrator/source-evidence regressions (632) and focused OMI/project regressions (109) still pass, and the BookNLP/spaCy availability guard reports both false. `PHASE8-IMPL-012-T006` validated the review queue storage safety boundaries as a validation-only pass: the target review queue storage contract passes (359 tests), the candidate review gate contract passes (154 tests), candidate regressions pass (307 tests), the orchestrator contract passes (67 tests), the source/evidence contract passes (104 tests), parser/storage/adapter regressions pass (64 + 185 + 148 tests), and focused OMI/project regressions pass (109 tests); the BookNLP/spaCy availability guard reports both false; no safety gap was found, so no runtime hardening patch was needed and `backend/story_knowledge/review_queue_storage.py` was left unchanged. The child sequence is T001 parent publication (complete), T002 review queue storage contract decision (complete), T003 owner action workflow boundary decision (complete), T004 review queue storage contract tests (complete), T005 minimal review queue storage helper if authorized (complete), T006 review queue safety regression or conditional hardening (complete), and T007 roadmap/status closeout (complete). `PHASE8-IMPL-012-T007` recommended `PHASE8-IMPL-013 - Writer Assistant Core review UI/API planning and owner-action workflow contract` as the next parent (recommendation-only, not active until separately published).
- T001 boundary: docs/status/planning only; no backend code, frontend code, tests, project files, training files, package/dependency files, review queue storage, owner action workflow, review UI/API, backend routes, frontend UI, apply-promotion, memory/canon mutation, raw artifact persistence, runtime extraction, real BookNLP/spaCy install/run/import, or generated prose behavior was added or authorized.
- Runtime boundary: review queue entries remain workflow support only; `PHASE8-IMPL-012-T005` added a pure project-local candidate-linked review-workflow-only queue storage helper (`backend/story_knowledge/review_queue_storage.py`) that validates, writes, reads, lists, and derives a rebuildable index for queue entries under `writer_assistant/review_queue/` (exercised only in tmp_path tests), plus owner action record shape validation only. Queue presence is non-approval. Candidate records remain candidate-only/review-pending and owner review remains mandatory before anything can become approved truth. `PHASE8-IMPL-012-T006` was validation-only and changed no runtime code; it confirmed the helper stays inside these boundaries. No owner action workflow execution, owner action storage, review UI/API, backend routes, frontend UI, apply-promotion, memory/canon mutation, raw artifact persistence, or runtime extraction exists yet.
- Last completed parent note: `PHASE8-IMPL-011` is complete. The planning/contract parent for candidate review queue and persistence gate boundaries delivered candidate draft validation, a candidate-only persistence gate, and an in-memory review queue entry builder. `PHASE8-IMPL-011-T005` implemented the minimal candidate persistence gate helper `backend/story_knowledge/candidate_review_gate.py`, a pure, standard-library-only, deterministic, candidate-only/review-pending gate over existing candidate validation/persistence helpers, exposing `validate_candidate_draft_for_persistence`, `build_candidate_record_from_draft`, `build_review_queue_entry`, and `persist_candidate_record_for_review`. `PHASE8-IMPL-011-T006` validated the candidate review gate safety boundaries as a validation-only pass: the target contract passes (154 tests), candidate regressions pass (307 tests), orchestrator/source-evidence contracts pass (171 tests), parser/storage/adapter regressions pass (397 tests), and focused OMI/project regressions pass (109 tests). No safety gap was found, so no runtime hardening patch was needed and the helper was left unchanged. `PHASE8-IMPL-011-T007` closed the parent as docs/status closeout only and recommended `PHASE8-IMPL-012` as the next parent, now published and active.
- Prior frontier note: `PHASE8-IMPL-010` is complete through T007 and delivered the review-safe fixture orchestrator and boundary decisions.
- Prior completed child task: `PHASE8-IMPL-010-T007` - Roadmap/status closeout.
- Prior completed child task: `PHASE8-IMPL-009-T007` - Roadmap/status closeout.
- Prior completed child task: `PHASE8-IMPL-010-T005` - Candidate review handoff and persistence boundary decision.
- Prior completed child task: `PHASE8-IMPL-010-T004` - Minimal review-safe orchestration helper.
- Prior completed child task: `PHASE8-IMPL-010-T003` - Extraction orchestration contract tests.
- Prior completed child task: `PHASE8-IMPL-010-T002` - Review-safe extraction pipeline contract decision.
- Prior completed child task: `PHASE8-IMPL-009-T006` - Targeted parser contract validation and boundary hardening.
- Prior completed child task: `PHASE8-IMPL-009-T005` - Raw artifact bundle builder integration with adapter/storage/source/evidence contracts.
- Prior completed child task: `PHASE8-IMPL-009-T004` - Book JSON parsing and token-event derivation implementation.
- Prior completed child task: `PHASE8-IMPL-009-T003` - Minimal BookNLP TSV fixture parser implementation.
- Prior completed child task: `PHASE8-IMPL-009-T002` - Parser implementation contract reconciliation decision.
- Prior completed child task: `PHASE8-IMPL-009-T001` - Publish BookNLP fixture parser implementation parent.
- Last completed child under prior parent: `PHASE8-IMPL-008-T007` - Roadmap/status closeout.
- Prior completed parent task: `PHASE8-IMPL-008` - Writer Assistant Core raw extraction artifact storage and BookNLP fixture parser contract.
- Prior completed child under last parent: `PHASE8-IMPL-004-T006` - Index safety repair or hardening (validation-only).
- Prior completed child under last parent: `PHASE8-IMPL-003-T006` - Candidate list/index helper implementation.
- Prior completed child under last parent: `PHASE8-IMPL-003-T005` - Candidate list/index contract tests.
- Prior completed child under last parent: `PHASE8-IMPL-003-T004` - Minimal candidate persistence helpers.
- Prior completed child under last parent: `PHASE8-IMPL-003-T003` - Candidate persistence write/read contract tests.
- Prior completed child under last parent: `PHASE8-IMPL-003-T002` - Candidate persistence contract decision.
- Prior completed child under last parent: `PHASE8-IMPL-003-T001` - Publish candidate persistence parent and child-task plan.
- Prior completed parent task: `PHASE8-IMPL-002` - Writer Assistant Core candidate storage contract and evidence/provenance validation.
- Prior completed child under prior parent: `PHASE8-IMPL-002-T007` - Roadmap/status closeout.
- Prior completed child under last parent: `PHASE8-IMPL-002-T006` - Candidate storage helper skeleton.
- Prior completed child under last parent: `PHASE8-IMPL-002-T005` - Project-local candidate storage path contract tests.
- Prior completed child under last parent: `PHASE8-IMPL-002-T004` - Candidate record validation helpers.
- Prior completed child under last parent: `PHASE8-IMPL-002-T003` - Candidate storage/evidence contract tests.
- Prior completed child under last parent: `PHASE8-IMPL-002-T002` - Candidate storage and evidence/provenance contract decision.
- Prior completed child under last parent: `PHASE8-IMPL-002-T001` - Publish candidate storage/evidence validation parent and child-task plan.
- Prior completed parent task: `PHASE8-IMPL-001` - Writer Assistant Core implementation readiness and first runtime slice plan.
- Prior completed child under prior parent: `PHASE8-IMPL-001-T007` - Roadmap/status closeout.
- Prior completed child: `PHASE8-IMPL-001-T006` - Writer Assistant Core candidate schema constants.
- Prior completed child: `PHASE8-IMPL-001-T005` - Writer Assistant Core candidate schema contract tests.
- Prior completed child: `PHASE8-IMPL-001-T004` - First runtime slice decision.
- Prior completed child: `PHASE8-IMPL-001-T003` - Targeted context collection and source inventory.
- Prior completed child: `PHASE8-IMPL-001-T002` - Context collection plan for Writer Assistant Core.
- Prior completed child: `PHASE8-IMPL-001-T001` - Publish Writer Assistant Core parent and child-task plan.
- Prior completed child: `PHASE7-IMPL-010-T006` - Final validation regression pass.
- Prior completed child: `PHASE7-IMPL-010-T005` - Validation repair triage.
- Prior completed child: `PHASE7-IMPL-010-T004` - Browser/manual smoke execution (PARTIAL).
- Prior completed parent task: `PHASE7-IMPL-010` - Workspace Validation / Browser and Manual Smoke.
- Completed parent task: `PHASE7-IMPL-010` - Workspace Validation / Browser and Manual Smoke.
- Completed parent task: `PHASE7-IMPL-009` - Memory / Canon shell (approved-only empty states).
- Completed parent task: `PHASE7-IMPL-008` - OMI-guided project creation staged flow.
- Completed parent task: `PHASE7-IMPL-007` - Project Overview shell.
- Completed parent task: `PHASE7-IMPL-006` - Shared owner-authored document editor.
- Completed parent task: `PHASE7-IMPL-005` - Notes / Materials Storage.
- Completed closeout micro-task: `PHASE7-IMPL-010-T007` - Roadmap/status closeout.
- Phase 7 published sequence is complete (`PHASE7-IMPL-001` through `PHASE7-IMPL-010`).
- Next child: none published; next parent/child requires owner/roadmap confirmation.
- Deferred validation: interactive browser flows D, E, H, I, J, K, L when Playwright deps or browser MCP are available; note/material flows F/G unless controlled fixtures are authorized later.
- Local smoke artifact (uncommitted): `projects/smoke-blank-1781586974/`.

`PHASE8-IMPL-001` is complete as the first Writer Assistant Core readiness parent. T001 published the parent and child-task plan. T002 published the context collection plan at `docs/roadmap/context/PHASE8-IMPL-001-context-collection-plan.md`. T003 produced the targeted context report at `docs/roadmap/context/PHASE8-IMPL-001-targeted-context-report.md` using direct file/source inspection only. T004 accepted `docs/roadmap/decisions/PHASE8-IMPL-001-first-runtime-slice-decision.md` and selected Writer Assistant Core candidate schema constants plus source-level/contract tests as the first runtime slice. T005 added tests-first contract coverage in `tests/test_writer_assistant_core_candidate_schema_contract.py`. T006 added constants-only schema metadata in `backend/story_knowledge/candidate_schema.py` and satisfied the T005 contract. T007 closed the parent with final validation. PHASE8-IMPL-001 completed schema contract/constants only and did not add extraction, candidate creation runtime, backend routes, frontend extraction UI, model/Ollama calls, apply-promotion, memory/canon mutation, package changes, candidate storage writes, training data, JSONL records, or dataset artifacts.

`PHASE8-IMPL-002` is complete as the second Writer Assistant Core parent. It prepared candidate storage contracts and evidence/provenance validation before extraction/runtime expansion. `PHASE8-IMPL-002-T001` published the parent, inventory, enrichment JSON, child-task plan, and roadmap/status updates. `PHASE8-IMPL-002-T002` accepted the storage record contract in `docs/roadmap/decisions/PHASE8-IMPL-002-candidate-storage-evidence-contract-decision.md`, selected T003 as tests-only, deferred validation helpers to T004, and deferred storage writes until T005/T006. `PHASE8-IMPL-002-T003` added tests-only contract coverage in `tests/test_writer_assistant_core_candidate_record_contract.py` for record shape, source locator, evidence/provenance, status/destination, path safety, and forbidden mutation. `PHASE8-IMPL-002-T004` added pure validation helpers in `backend/story_knowledge/candidate_record.py` and satisfied the T003 contract tests with no file I/O. `PHASE8-IMPL-002-T005` added tests-only storage path contract coverage in `tests/test_writer_assistant_core_candidate_storage_contract.py` for `writer_assistant/candidates/` and `writer_assistant/index.json` boundaries. `PHASE8-IMPL-002-T006` added pure path helpers in `backend/story_knowledge/candidate_storage.py` and satisfied the T005 storage contract tests. `PHASE8-IMPL-002-T007` closed the parent with final validation. Final behavior: candidate record validation helpers are pure; storage helpers are pure path helpers only; no directories/files are created; no JSON read/write/list behavior exists. Phase 8 still has no extraction runtime, no candidate JSON persistence, no backend extraction routes, no frontend extraction/review UI, no model/Ollama calls, no apply-promotion, and no memory/canon mutation.

`PHASE8-IMPL-003` is complete as the third Writer Assistant Core parent. `PHASE8-IMPL-003-T001` published the parent, inventory, enrichment JSON, child-task plan, and roadmap/status updates. `PHASE8-IMPL-003-T002` accepted the candidate persistence contract decision at `docs/roadmap/decisions/PHASE8-IMPL-003-candidate-persistence-contract-decision.md`, selected T003 as tests-only write/read, T004 as minimal write/read helpers, T005 as tests-only list, T006 as list helper only if T005 authorizes, and deferred index read/write to a later parent. `PHASE8-IMPL-003-T003` added tests-only write/read persistence contract coverage in `tests/test_writer_assistant_core_candidate_persistence_contract.py`. `PHASE8-IMPL-003-T004` created `backend/story_knowledge/candidate_persistence.py` with minimal candidate-only JSON write/read helpers and satisfied the targeted persistence contract test. `PHASE8-IMPL-003-T005` added tests-only list contract coverage in `tests/test_writer_assistant_core_candidate_list_contract.py`. `PHASE8-IMPL-003-T006` added `list_candidate_records` to `backend/story_knowledge/candidate_persistence.py` and satisfied the targeted list contract test. `PHASE8-IMPL-003-T007` closed the parent with final validation. Final behavior: `write_candidate_record`, `read_candidate_record`, and `list_candidate_records` provide candidate-only JSON persistence; candidate JSON files under `writer_assistant/candidates/*.json` are source of truth; write validates before persistence; read/list validate loaded records and are side-effect free; list is direct-file-only, non-recursive, and deterministic by `candidate_id`; no index is read/written/created. T005/T006 are list-only despite the child labels mentioning list/index; index read/write was deferred to `PHASE8-IMPL-004`.

`PHASE8-IMPL-004` is complete as the fourth Writer Assistant Core parent. `PHASE8-IMPL-004-T001` published the parent, inventory, enrichment JSON, child-task plan, and roadmap/status updates. `PHASE8-IMPL-004-T002` accepted the derived index contract decision at `docs/roadmap/decisions/PHASE8-IMPL-004-candidate-index-contract-decision.md`, selected T003 as tests-only index contract coverage, T004 as minimal index helpers in `backend/story_knowledge/candidate_index.py`, and deferred incremental sync/update APIs, routes, UI, extraction, model calls, apply-promotion, and memory/canon mutation. `PHASE8-IMPL-004-T003` added tests-only index contract coverage in `tests/test_writer_assistant_core_candidate_index_contract.py`. `PHASE8-IMPL-004-T004` created `backend/story_knowledge/candidate_index.py` with `build_candidate_index`, `write_candidate_index`, and `read_candidate_index`; derived index helpers use candidate JSON as source of truth and T003 index contract tests now pass. `PHASE8-IMPL-004-T005` added focused regression coverage in `tests/test_writer_assistant_core_candidate_index_safety_regression.py`; targeted regression pytest passes with no repair gap. `PHASE8-IMPL-004-T006` completed as validation-only because T005 found no repair or hardening gaps; T004 index helpers already satisfy T005 safety/stale/corrupt regression coverage. `PHASE8-IMPL-004-T007` closed the parent with final validation. Final behavior: `build_candidate_index`, `write_candidate_index`, and `read_candidate_index` provide derived candidate index helpers; candidate JSON files under `writer_assistant/candidates/{candidate_id}.json` remain source of truth; `writer_assistant/index.json` is derived convenience metadata only; build derives from `list_candidate_records` and is side-effect free; write refreshes index from candidate JSON; read validates index shape only and does not rebuild/repair; stale-but-valid index can be read as-is; corrupt index raises `ValueError` on read and is ignored by build; corrupt index may be overwritten by write if candidate JSON records validate. Writer Assistant Core currently has schema constants, candidate record validation, candidate storage path helpers, candidate JSON write/read/list persistence, and derived candidate index build/write/read helpers. Writer Assistant Core still has no backend routes for candidate review/index, frontend candidate review/backlog UI, runtime extraction, model/Ollama calls, semantic search, apply-promotion, or memory/canon mutation. No extraction implementation has started. The next work is tool evaluation and extraction strategy for exactly nine approved tools/references, not runtime extraction.

`PHASE8-IMPL-005` is complete as the fifth Writer Assistant Core parent. `PHASE8-IMPL-005-T001` published the parent, inventory, enrichment JSON, child-task plan, and roadmap/status updates. It recorded the narrowed nine-tool scope (dramatica-flow, Narrative Context Protocol, Subtxt docs, spaCy, segram, BookNLP, GLiNER, LangExtract, Renard) and the official source retrieval policy for later children. T001 did not evaluate tools, retrieve sources, install tools, or change runtime code/tests. `PHASE8-IMPL-005-T002` accepted the evaluation scope, fixture category plan, 0-5 scoring rubric with hard blockers and pass/fail gates, provisional tool grouping, and T003 source inventory requirements in `docs/roadmap/decisions/PHASE8-IMPL-005-evaluation-scope-fixture-rubric-decision.md`. T002 did not retrieve sources, create fixtures, install tools, or change runtime code/tests. `PHASE8-IMPL-005-T003` completed the official source inventory and license/dependency screen at `docs/roadmap/inventory/PHASE8-IMPL-005-tool-source-inventory.md` using Cursor web access and read-only terminal fetch of official docs/repos only; preliminary classifications: spaCy likely runtime adapter candidate; segram, BookNLP, GLiNER, LangExtract, and Renard possible runtime adapter candidates; dramatica-flow, NCP, and Subtxt docs reference-only; dramatica-flow B1 generation risk blocks runtime adapter. T003 did not install, clone, or execute tools; did not change runtime code/tests. `PHASE8-IMPL-005-T004` accepted dramatica-flow as `reference-only` for analysis-pattern/rubric inspiration in `docs/roadmap/decisions/PHASE8-IMPL-005-dramatica-flow-analysis-only-reference-decision.md`. Runtime adapter use is REJECT/DEFER. Safe reference concepts (causal chain, foreshadowing/promise lifecycle, emotional arc, relationship delta, timeline/thread activity, information boundaries, audit dimensions without revision) may inform future app-owned candidate rubrics only. Writer/Reviser/Architect generation, prose/outline generation, continuation, rewrite/revise APIs, world_state writes, and automatic canon settlement are rejected. T004 did not install, clone, or execute dramatica-flow; did not change runtime code/tests. `PHASE8-IMPL-005-T005` accepted NCP as `reference-only` and future `approved-context import/export candidate`, and Subtxt docs as `reference-only` and future `semantic rubric candidate`, in `docs/roadmap/decisions/PHASE8-IMPL-005-ncp-subtxt-structural-interpretation-strategy-decision.md`. Runtime adapter/import/export/Subtxt analysis implementation is REJECT/DEFER. NCP import as automatic truth, NCP export of raw candidates as final truth, automatic storyform labeling, and Subtxt/Dramatica labels without evidence are rejected. T005 did not install, clone, or execute tools; did not implement NCP import/export or Subtxt analysis; did not change runtime code/tests. `PHASE8-IMPL-005-T006` accepted the NLP/extraction adapter strategy decision at `docs/roadmap/decisions/PHASE8-IMPL-005-nlp-extraction-adapter-strategy-decision.md`: first extraction strategy is spaCy-first local deterministic/rule-assisted candidate extraction foundation; first implementation style is app-owned, local, deterministic/rule-assisted, and candidate-only. The first slice should focus on source segmentation/source locators, simple entity candidates, source evidence spans, candidate normalization, and no-prose/no-canon-mutation contracts. segram, BookNLP, GLiNER, LangExtract, and Renard are deferred from the first implementation slice; LangExtract remains later/local-only/model-assisted only if privacy/local model policy is approved, and Renard remains deferred pending GPL/license review and relationship-network scope. dramatica-flow, NCP, and Subtxt remain reference-only according to T004/T005. `PHASE8-IMPL-005-T007` closed the parent and recommends `PHASE8-IMPL-006 - Writer Assistant Core analysis-only candidate extraction architecture and spaCy-first local extraction foundation` as the next parent. No runtime extraction exists yet. PHASE8-IMPL-005 did not install, clone, execute, or locally evaluate external tools; did not change runtime code/tests/packages; and did not add backend routes, frontend UI, project runtime files, model calls, apply-promotion, memory/canon mutation, training data, JSONL records, or dataset files.

`PHASE8-IMPL-006` is complete as the sixth Writer Assistant Core parent. `PHASE8-IMPL-006-T001` published the evidence-first extraction foundation and BookNLP-ready adapter strategy with docs/status/planning artifacts only. `PHASE8-IMPL-006-T002` accepted the evidence/source-map contract decision at `docs/roadmap/decisions/PHASE8-IMPL-006-evidence-source-map-contract-decision.md`. The accepted contract defines app-owned source document identity, source maps, source locators, primary character offsets over exact UTF-8 decoded source snapshots, source hashes/snapshots, Evidence Ledger shape, evidence records, extraction run provenance, raw output policy, BookNLP-ready mapping assumptions, candidate normalization boundaries, and owner-review/no-canon guardrails before runtime extraction. `PHASE8-IMPL-006-T003` added tests-first source/evidence contract coverage in `tests/test_writer_assistant_core_source_evidence_contract.py`. `PHASE8-IMPL-006-T004` added pure validation helpers in `backend/story_knowledge/source_map.py` and `backend/story_knowledge/evidence.py`; the source/evidence contract tests now pass. `PHASE8-IMPL-006-T005` accepted the BookNLP-ready raw output and adapter contract decision at `docs/roadmap/decisions/PHASE8-IMPL-006-booknlp-ready-raw-output-adapter-contract-decision.md`. `PHASE8-IMPL-006-T006` added tests-first BookNLP-ready adapter contract coverage in `tests/test_writer_assistant_core_booknlp_adapter_contract.py` for future `backend.story_knowledge.booknlp_adapter_contract` APIs: `validate_booknlp_run_manifest`, `validate_booknlp_raw_artifact_bundle`, `normalize_booknlp_entity_mentions`, `normalize_booknlp_quotes`, `normalize_booknlp_events`, and `build_booknlp_candidate_drafts`. `PHASE8-IMPL-006-T007` closed the parent. Final behavior now available: pure source document reference, source segment, source map, source locator, evidence record, extraction run provenance, and raw output reference validation; source/evidence contract tests pass; existing Writer Assistant Core candidate contract regressions pass. The BookNLP adapter contract test remains expected red until future `backend.story_knowledge.booknlp_adapter_contract` exists. No runtime extraction exists yet. BookNLP and spaCy are not installed or run. No production adapter code exists yet. Recommended next parent: `PHASE8-IMPL-007` - Writer Assistant Core BookNLP adapter contract implementation and mocked normalization foundation.

`PHASE8-IMPL-007` is complete as the seventh Writer Assistant Core parent. `PHASE8-IMPL-007-T001` published the parent task record, inventory, enrichment JSON, and roadmap/status updates and confirmed the four `.external_sources/` clone paths (`.external_sources/booknlp`, `.external_sources/dramatica-flow`, `.external_sources/narrative-context-protocol`, `.external_sources/subtxt-docs`) exist locally and are protected from commit via `.git/info/exclude`. `PHASE8-IMPL-007-T002` completed the read-only official source inventory from `.external_sources/`, created `docs/roadmap/inventory/PHASE8-IMPL-007-official-source-inventory.md`, created `docs/roadmap/decisions/PHASE8-IMPL-007-implementation-source-refresh-decision.md`, and verified local repo SHAs for BookNLP (`3d900fc2224e55960c3363826ae28539b77b4204`), dramatica-flow (`890f099bfcb64adbf407fd83ab708c48e92b0766`), Narrative Context Protocol (`b1222748376aae3d309176b3bb5afb884eb281ea`), and Subtxt docs (`ec66121364c039693314dcce4cde464e497bece4`). T002 confirmed BookNLP real output fields and refined `booknlp_events` as an app-owned derived abstraction from `.tokens.event`; confirmed dramatica-flow runtime remains blocked/deferred; confirmed NCP remains future approved-context import/export reference only; and confirmed Subtxt docs remain semantic guardrail/reference only. `PHASE8-IMPL-007-T003` completed the docs-only implementation decision at `docs/roadmap/decisions/PHASE8-IMPL-007-booknlp-adapter-implementation-decision.md`, accepting T004 validators/API symbols first, T005 mocked normalizers, and T006 builder/boundary hardening. `PHASE8-IMPL-007-T004` created `backend/story_knowledge/booknlp_adapter_contract.py` as a pure mocked standard-library-only module, exposed all six public APIs, implemented run manifest and raw artifact bundle validators, and added minimal in-memory draft shaping required by the current contract tests. `PHASE8-IMPL-007-T005` completed as validation-only after reviewing entity/quote/event mocked normalization and confirming no repair gap: drafts remain in-memory only, source/evidence/provenance-backed, candidate support only, and fail closed for unsupported or ambiguous records. `PHASE8-IMPL-007-T006` completed as validation-only after reviewing `build_booknlp_candidate_drafts`, fail-closed behavior, and boundary hardening; the current implementation already combines mocked entity/quote/event drafts in memory, avoids promotion and forbidden destinations, preserves source/evidence/provenance/raw refs, and adds no persistence, canon, memory, project-file, runtime extraction, route, UI, package, or test changes. `PHASE8-IMPL-007-T007` closed the parent. Final behavior now available: `validate_booknlp_run_manifest`, `validate_booknlp_raw_artifact_bundle`, `normalize_booknlp_entity_mentions`, `normalize_booknlp_quotes`, `normalize_booknlp_events`, and `build_booknlp_candidate_drafts`; pure mocked manifest validation; pure mocked raw artifact bundle validation; pure mocked entity/quote/event draft normalization; pure mocked candidate draft builder; fail-closed behavior; and source/evidence/provenance/raw-ref integration. All runtime behavior remains bounded to in-memory mocked dictionaries. Real BookNLP/spaCy install or execution remains deferred, and no external repo code has been executed or vendored. No runtime extraction, backend routes, frontend UI, package/dependency changes, model calls, generated prose, apply-promotion, memory/canon mutation, project runtime files, or training/JSONL/dataset work was added. Recommended next parent, not active until separately published: `PHASE8-IMPL-009` - Writer Assistant Core BookNLP fixture parser helper implementation and raw artifact bundle integration.

`PHASE8-IMPL-008` is complete as the eighth Writer Assistant Core parent. `PHASE8-IMPL-008-T001` published the parent task record at `docs/roadmap/tasks/PHASE8-IMPL-008.md`, the inventory at `docs/roadmap/inventory/PHASE8-IMPL-008.md`, the enrichment JSON at `docs/roadmap/enrichment/PHASE8-IMPL-008.enrichment.json`, and the roadmap/status updates. T001 confirmed the four `.external_sources/` clone paths exist locally and remain protected from commit via `.git/info/exclude`, and confirmed `backend/story_knowledge/booknlp_adapter_contract.py` and `tests/test_writer_assistant_core_booknlp_adapter_contract.py` are unchanged. `PHASE8-IMPL-008-T002` completed the raw extraction artifact storage contract decision at `docs/roadmap/decisions/PHASE8-IMPL-008-raw-extraction-artifact-storage-contract-decision.md`. T002 accepted the future storage root `project_dir / "writer_assistant" / "extractions"`, run-folder shape `writer_assistant/extractions/{tool_name}/{run_id}/`, first tool folder `booknlp`, strict tool/run ID rules, manifest shape and policy flags, raw artifact refs through existing source/evidence boundaries where compatible, `raw/` BookNLP artifact layout, optional raw-ref-only `book.html`, and `derived/events.json` only as app-owned derived support from `.tokens.event`, not as a real external BookNLP raw output file. `PHASE8-IMPL-008-T003` added expected-red tests-only contract coverage at `tests/test_writer_assistant_core_raw_extraction_storage_contract.py` for the future `backend.story_knowledge.raw_extraction_storage` module and APIs `validate_extraction_storage_id`, `extraction_storage_dir`, `tool_extraction_dir`, `extraction_run_dir`, `extraction_manifest_path`, `raw_artifact_dir`, `raw_artifact_path`, `derived_artifact_dir`, `validate_extraction_storage_path`, and `validate_extraction_run_manifest`. `PHASE8-IMPL-008-T004` created `backend/story_knowledge/raw_extraction_storage.py` as a pure standard-library storage path and manifest validation helper module; the T003 storage contract now passes. T004 implemented no raw file write/read/list helpers, no parser helpers, no runtime extraction, no project runtime file creation, no real BookNLP/spaCy install or execution, no routes, no UI, and no package/dependency changes. `PHASE8-IMPL-008-T005` completed the BookNLP fixture parser contract decision at `docs/roadmap/decisions/PHASE8-IMPL-008-booknlp-fixture-parser-contract-decision.md`. T005 accepted the future in-memory only fixture parser contract: future module `backend/story_knowledge/booknlp_fixture_parser.py`; future APIs `parse_booknlp_tokens_tsv`, `parse_booknlp_entities_tsv`, `parse_booknlp_quotes_tsv`, `parse_booknlp_supersense_tsv`, `parse_booknlp_book_json`, `build_booknlp_raw_artifact_bundle_from_fixture_texts`, and optional `derive_booknlp_events_from_tokens`; exact TSV header validation against T002 source inventory columns; numeric coercion with fail-closed behavior; `.book` JSON parsing with `g` as raw aggregate metadata only; event derivation from `.tokens.event` only and no `.events` raw input; bundle builder compatibility with `validate_booknlp_raw_artifact_bundle`; T006 tests-first only and expected-red; parser implementation deferred to `PHASE8-IMPL-009`. `PHASE8-IMPL-008-T006` added `tests/test_writer_assistant_core_booknlp_fixture_parser_contract.py` as expected-red contract coverage for the future parser module and APIs. The target parser pytest fails during collection with `ImportError: cannot import name 'booknlp_fixture_parser' from 'backend.story_knowledge'`, limited to the missing future module/symbol. T006 covers TSV parsing, numeric validation, `.book` JSON validation, event derivation, raw artifact bundle builder compatibility, storage/source/evidence helper integration, raw output reference boundaries, and a future production source-level boundary scan. `PHASE8-IMPL-008-T007` closed the parent as complete. No child remains under this parent. No raw extraction artifact files exist, no BookNLP fixture parser exists yet, no parser implementation was added, no runtime extraction exists, no real BookNLP/spaCy install/run/import occurred, no generated prose, no apply-promotion, and no memory/canon mutation exists. Owner review remains mandatory.

`PHASE8-IMPL-009` is complete as the ninth Writer Assistant Core parent. `PHASE8-IMPL-009-T001` published the parent task record at `docs/roadmap/tasks/PHASE8-IMPL-009.md`, the inventory at `docs/roadmap/inventory/PHASE8-IMPL-009.md`, the enrichment JSON at `docs/roadmap/enrichment/PHASE8-IMPL-009.enrichment.json`, and the roadmap/status updates. `PHASE8-IMPL-009-T002` completed the parser implementation contract reconciliation decision at `docs/roadmap/decisions/PHASE8-IMPL-009-parser-implementation-contract-reconciliation-decision.md`, accepted the public parser APIs, and accepted the T003-T006 implementation split. `PHASE8-IMPL-009-T003` created `backend/story_knowledge/booknlp_fixture_parser.py` and implemented pure in-memory TSV parsers for tokens, entities, quotes, and supersense. `PHASE8-IMPL-009-T004` implemented pure in-memory `.book` JSON parsing and token-event derivation from token `event` markers, preserving `.book.g` as raw aggregate metadata only and derived events as support only. `PHASE8-IMPL-009-T005` implemented pure in-memory raw artifact bundle builder integration in `backend/story_knowledge/booknlp_fixture_parser.py`, validating fixture input, storage manifest, source map, raw output references, derived token events, and the final adapter-compatible bundle. `PHASE8-IMPL-009-T006` completed validation and boundary hardening with the full parser contract passing, no parser hardening patch needed, and existing storage/adapter/source/evidence/candidate/OMI regressions green. `PHASE8-IMPL-009-T007` completed roadmap/status closeout and recorded tracked-artifact confirmation. Final behavior now available: pure standard-library in-memory BookNLP TSV fixture parsing, pure in-memory `.book` JSON parsing, event derivation from token `event` column, and raw artifact bundle construction from in-memory fixture strings. No active child remains under `PHASE8-IMPL-009`. No parser filesystem I/O, raw artifact persistence, raw write/read/list helpers, runtime extraction, routes, UI, package changes, real BookNLP/spaCy runtime, generated prose, apply-promotion, or memory/canon mutation exists.

`PHASE8-IMPL-011` is complete as the eleventh Writer Assistant Core parent. `PHASE8-IMPL-011-T001` published the parent task record at `docs/roadmap/tasks/PHASE8-IMPL-011.md`, the inventory at `docs/roadmap/inventory/PHASE8-IMPL-011.md`, the enrichment JSON at `docs/roadmap/enrichment/PHASE8-IMPL-011.enrichment.json`, and the roadmap/status updates as docs/status/planning only. `PHASE8-IMPL-011-T002` is complete as docs/decision only and accepted the candidate draft to candidate record persistence gate decision at `docs/roadmap/decisions/PHASE8-IMPL-011-candidate-draft-to-record-persistence-gate-decision.md`. `PHASE8-IMPL-011-T003` is complete as docs/decision only and accepted the review queue data shape and lifecycle decision at `docs/roadmap/decisions/PHASE8-IMPL-011-review-queue-data-shape-lifecycle-decision.md`. Future queue entries are workflow support only, queue presence is non-approval, lifecycle states are review workflow states only, evidence/provenance/source locator display is mandatory, insufficient-evidence/rejected-output handling must be explicit, grouping/sorting/filtering are workflow convenience only, and owner action labels are future planning terms only. `PHASE8-IMPL-011-T004` is complete as tests-first only and added expected-red candidate persistence gate contract tests at `tests/test_writer_assistant_core_candidate_review_gate_contract.py` for the future `backend.story_knowledge.candidate_review_gate` module and its `validate_candidate_draft_for_persistence`, `build_candidate_record_from_draft`, `build_review_queue_entry`, and `persist_candidate_record_for_review` APIs; the target pytest is expected-red with a collection `ImportError` limited to the missing future module, and candidate/orchestrator/source-evidence/OMI/project regressions pass. `PHASE8-IMPL-011-T005` is complete and created `backend/story_knowledge/candidate_review_gate.py`, a pure, standard-library-only, deterministic, candidate-only/review-pending persistence gate over existing candidate validation/persistence helpers, implementing `validate_candidate_draft_for_persistence`, `build_candidate_record_from_draft`, `build_review_queue_entry`, and `persist_candidate_record_for_review`; the target contract passes (154 tests) and candidate/orchestrator/source-evidence/OMI/project regressions pass. `PHASE8-IMPL-011-T006` is complete as a validation-only candidate review gate safety regression: the target gate contract passes (154 tests), candidate regressions pass (307 tests), orchestrator/source-evidence contracts pass (171 tests), parser/storage/adapter regressions pass (397 tests), and focused OMI/project regressions pass (109 tests); the BookNLP/spaCy availability guard reports both false with no gate import; no safety gap was found, so no runtime hardening patch was needed and `candidate_review_gate.py` was left unchanged. `PHASE8-IMPL-011-T007` is complete as docs/status closeout only and records the final parent result, final artifacts, final APIs, final runtime/test behavior, deferred work, tracked-artifact confirmation, validation results, and the next-parent recommendation `PHASE8-IMPL-012 - Writer Assistant Core review queue storage and owner-review workflow planning` (recommendation-only, not active until separately published); T007 changed no runtime code, tests, routes, UI, packages, project files, training files, raw artifacts, apply-promotion, memory/canon state, or generated prose behavior. T001-T007 added no automatic candidate persistence from orchestrator output, no candidate review queue storage/listing, no candidate review UI/API, no backend routes, no frontend UI, no raw artifact persistence, no apply-promotion, no memory/canon mutation, no runtime extraction, no real BookNLP/spaCy install or run, no package/dependency changes, no generated prose, and no training/JSONL/dataset work; T005 added only the narrowly tested candidate-only persistence through existing helpers, and T006/T007 changed no runtime code. Candidate drafts remain in-memory review support only, persisted records remain candidate-only/review-pending, and owner review remains mandatory before anything can become approved truth. No active child remains under `PHASE8-IMPL-011`.

`PHASE8-IMPL-012` is complete as the twelfth Writer Assistant Core parent. `PHASE8-IMPL-012-T001` published the parent task record, inventory, and enrichment JSON as docs/status/planning only. `PHASE8-IMPL-012-T002` accepted the review queue storage contract decision at `docs/roadmap/decisions/PHASE8-IMPL-012-review-queue-storage-contract-decision.md`. `PHASE8-IMPL-012-T003` accepted the owner action workflow boundary decision at `docs/roadmap/decisions/PHASE8-IMPL-012-owner-action-workflow-boundary-decision.md` (owner actions are review workflow commands and states only, not apply-promotion, not memory/canon mutation, and not approval/canon truth). `PHASE8-IMPL-012-T004` added expected-red review queue storage contract tests at `tests/test_writer_assistant_core_review_queue_storage_contract.py`. `PHASE8-IMPL-012-T005` created `backend/story_knowledge/review_queue_storage.py`, a pure, standard-library-only, deterministic, project-local, candidate-linked, candidate-only, review-workflow-only queue storage helper implementing `validate_review_queue_entry`, `build_review_queue_entry_from_candidate_record`, `review_queue_storage_dir`, `review_queue_entry_path`, `review_queue_index_path`, `write_review_queue_entry`, `read_review_queue_entry`, `list_review_queue_entries`, `build_review_queue_index`, and `validate_owner_action_record`; the target review queue storage contract passes (359 tests). `PHASE8-IMPL-012-T006` validated the review queue storage safety boundaries as a validation-only pass and required no runtime hardening patch, so the helper was left unchanged. `PHASE8-IMPL-012-T007` closed the parent as docs/status closeout only, confirmed tracked artifacts, re-validated the review queue storage contract (359 tests), candidate review gate contract (154 tests), candidate regressions (307 tests), orchestrator contract (67 tests), source/evidence contract (104 tests), parser/storage/adapter regressions (64 + 185 + 148 tests), and focused OMI/project regressions (109 tests) with the BookNLP/spaCy availability guard reporting both false, and recommended `PHASE8-IMPL-013 - Writer Assistant Core review UI/API planning and owner-action workflow contract` as the next parent (recommendation-only, not active until separately published). `PHASE8-IMPL-012` delivered review queue storage validation, a project-local queue storage helper, a derived index, and owner action record validation only. No review UI/API, backend routes, frontend UI, owner action workflow execution, orchestrator auto-persistence, apply-promotion, memory/canon mutation, raw artifact persistence, runtime extraction, real BookNLP/spaCy install/run/import, package/dependency changes, generated prose/rewrite/continuation, model calls, or training/JSONL/dataset work exists or was added; T005 added only the minimal pure review queue storage helper and T006/T007 changed no runtime code. No active child remains under `PHASE8-IMPL-012`.

`PHASE8-IMPL-013` is complete as the thirteenth Writer Assistant Core parent through `PHASE8-IMPL-013-T007`. It was a planning/contract parent that decided whether and how the project-local review queue storage and owner action record validation delivered by the completed `PHASE8-IMPL-012` review queue storage helper should be exposed through a read-only review queue API, an owner-action command API, and a frontend review UI, without implementing any backend routes, frontend screens, owner action execution, apply-promotion, or memory/canon mutation. T001 published the parent task record, inventory, and enrichment JSON as docs/status/planning only. T002 accepted the read-only review queue API contract decision at `docs/roadmap/decisions/PHASE8-IMPL-013-read-only-review-queue-api-contract-decision.md`. T003 accepted the owner action command API contract decision at `docs/roadmap/decisions/PHASE8-IMPL-013-owner-action-command-api-contract-decision.md`. T004 accepted the review UI planning boundary at `docs/roadmap/decisions/PHASE8-IMPL-013-review-ui-planning-boundary-decision.md`. T005 added expected-red review API contract tests only at `tests/test_writer_assistant_review_api_contract.py`. T006 validated that the expected-red handoff remains safe: the review API contract fails only because `backend.review_api` is missing; storage/gate/candidate/orchestrator/source-evidence/parser-storage-adapter/OMI regressions pass; `backend.review_api.py` and `backend/routes/review_queue.py` remain absent; no review literals were found in the active route/API/App/component files checked; BookNLP/spaCy availability guard reports both false; and no hardening patch was needed. T007 closed the parent as docs/status closeout only and recommends `PHASE8-IMPL-014 - Writer Assistant Core review API implementation and tests-first route boundary` as the next parent (recommendation-only, not active until separately published). Final artifacts include the three T002-T004 decision records and expected-red `tests/test_writer_assistant_review_api_contract.py` for future `backend.review_api`. Missing/deferred layers are `backend.review_api` implementation, backend read-only review queue routes, owner action command API routes, frontend API helpers, frontend review UI, frontend UI contract test harness, owner action execution, apply-promotion, memory/canon mutation, runtime extraction, raw artifact persistence, and generated prose. No review UI/API implementation, backend routes, frontend UI, frontend API helpers, owner action execution, apply-promotion, memory/canon mutation, candidate/canon/memory mutation, raw artifact persistence, runtime extraction, real BookNLP/spaCy install/run/import, package/dependency changes, generated prose/rewrite/continuation, model calls, raw artifact writes, or training/JSONL/dataset work is authorized or was added in T001-T007.

`PHASE8-IMPL-014` is complete as the fourteenth Writer Assistant Core parent through `PHASE8-IMPL-014-T007`. It was the tests-first-authorized implementation parent that satisfied the review API contract tests added by `PHASE8-IMPL-013-T005` (`tests/test_writer_assistant_review_api_contract.py` for `backend.review_api`), while preserving the boundaries from `PHASE8-IMPL-013-T002` (read-only review queue API contract), `PHASE8-IMPL-013-T003` (owner action command API contract), and `PHASE8-IMPL-013-T004` (review UI planning boundary). T001 published the parent task record, inventory, enrichment JSON, and roadmap/status updates as docs/status/planning only. T002 created the review API implementation reconciliation decision. T003 created `backend/review_api.py` with all seven public APIs present. T004 integrated read-only list/get/index/summary helpers over `review_queue_storage` via optional keyword-only `project_dir`. T005 hardened owner action command request validation and `build_owner_action_command_response`. T006 completed safety regression validation with no `backend/review_api.py` code change needed. T007 closed the parent as docs/status closeout only and recommends `PHASE8-IMPL-015` as the next MVP-required parent (recommendation-only until separately published). Final artifacts: `backend/review_api.py` with `list_review_queue_entries_readonly`, `get_review_queue_entry_readonly`, `get_review_queue_index_readonly`, `get_review_queue_summary_readonly`, `validate_review_queue_read_request`, `validate_owner_action_command_request`, and `build_owner_action_command_response`; review API contract tests pass (161 tests); review queue storage plus candidate review gate regressions pass (513 tests). No FastAPI route registration, frontend review UI/API helpers, owner action execution, apply-promotion, memory/canon mutation, raw artifact persistence, runtime extraction, real BookNLP/spaCy install/run/import, package/dependency changes, generated prose/rewrite/continuation, model calls, raw artifact writes, or training/JSONL/dataset work was added. No active child remains under `PHASE8-IMPL-014`.

`PHASE8-IMPL-015` is complete as the fifteenth Writer Assistant Core parent through `PHASE8-IMPL-015-T007`. It was the tests-first-authorized implementation parent that registered read-only review queue routes over the existing `backend.review_api` helper module and planned the read-only frontend review queue surface, while preserving the `PHASE8-IMPL-014` helper boundary, the `PHASE8-IMPL-013-T002` read-only review queue API contract, the `PHASE8-IMPL-013-T003` owner action command API contract, and the `PHASE8-IMPL-013-T004` review UI planning boundary. T001 published the parent task record, inventory, enrichment JSON, and roadmap/status updates as docs/status/planning only. T002 created the route/read-only frontend implementation reconciliation decision. T003 added expected-red route contract tests at `tests/test_writer_assistant_review_api_routes_contract.py`. T004 created `backend/routes/review_queue.py` with four GET-only read-only review queue routes and minimally included the router from `backend/main.py`; the route contract now passes (48 tests). T005 created the frontend read-only surface planning decision because no usable frontend/component test harness exists without package changes. T006 completed validation-only read-only safety regression with no hardening required. T007 closed the parent as docs/status closeout only and recommends `PHASE8-IMPL-016` as the next MVP-required parent (active after T001 publication). Final artifacts: `backend/routes/review_queue.py`; route contract tests PASS; review API contract tests PASS (161 tests); review queue storage plus candidate review gate regressions PASS (513 tests). No owner action command HTTP routes, owner action execution, apply-promotion, memory/canon mutation, raw artifact persistence, runtime extraction, model-assisted extraction, real BookNLP/spaCy install/run/import, NCP/Subtxt/dramatica-flow runtime integration, frontend production review surface, package/dependency changes, model calls, generated prose/prose-production paths, or training/JSONL/dataset work was added. No active child remains under `PHASE8-IMPL-015`.

`PHASE8-IMPL-016` is complete as the sixteenth Writer Assistant Core parent through `PHASE8-IMPL-016-T007`. It was the tests-first-authorized implementation parent that delivered the MVP-required frontend owner-action execution workflow and review command boundary for candidate review, while preserving the read-only review queue routes from `PHASE8-IMPL-015`, the owner-action command validation helpers from `PHASE8-IMPL-014`, and the boundary decisions from `PHASE8-IMPL-016-T002`. T001 published the parent task record, inventory, enrichment JSON, and roadmap/status updates as docs/status/planning only. T002 accepted the owner-action execution boundary decision. T003 added tests-first command route contract coverage at `tests/test_writer_assistant_review_action_command_routes_contract.py`. T004 implemented the minimal backend command route/helper for `POST /api/projects/{project_id}/review-queue/{queue_entry_id}/actions` as response-only review workflow command acceptance/rejection. T005 added the frontend owner-action review workflow/surface with deterministic API helper, read-only candidate details, explicit owner-action controls, candidate-only/no-canon warnings, and evidence/provenance/source locator display. T006 added focused safety regression coverage at `tests/test_owner_action_review_workflow_safety_regression.py`. T007 closed the parent as docs/status closeout only and recommends `PHASE8-IMPL-017` as the next MVP-required parent (ready/active pending T001 publication). Final artifacts: backend command route in `backend/routes/review_queue.py`; frontend helper and controls in `frontend/src/api.js`, `frontend/src/components/ReviewQueuePanel.jsx`, and `frontend/src/components/OwnerActionReviewControls.jsx`; command route contract PASS; frontend owner-action source tests PASS; safety regression PASS. No apply-promotion, approved memory/canon mutation, raw artifact persistence, runtime extraction, model-assisted extraction, real BookNLP/spaCy install/run/import, NCP/Subtxt/dramatica-flow runtime integration, queue/candidate persistence from command route, project truth mutation, package/dependency changes, model calls, generated prose/prose-production paths, or training/JSONL/dataset work was added. No active child remains under `PHASE8-IMPL-016`.

`PHASE8-IMPL-010` is complete as the tenth Writer Assistant Core parent. `PHASE8-IMPL-010-T001` published the parent task record at `docs/roadmap/tasks/PHASE8-IMPL-010.md`, the inventory at `docs/roadmap/inventory/PHASE8-IMPL-010.md`, the enrichment JSON at `docs/roadmap/enrichment/PHASE8-IMPL-010.enrichment.json`, and the roadmap/status updates. This parent is planning/contract-first for review-safe extraction orchestration. It decided how source document refs, source maps, evidence records, raw extraction storage path/manifest helpers, the in-memory BookNLP fixture parser, mocked/raw artifact bundle validation, BookNLP adapter normalization/draft support, candidate persistence/index helpers, and owner-review boundaries fit together before any real runtime extraction. `PHASE8-IMPL-010-T002` published the review-safe extraction pipeline contract decision at `docs/roadmap/decisions/PHASE8-IMPL-010-review-safe-extraction-pipeline-contract-decision.md`; T002 accepted the review-safe pipeline contract shape, the synthetic-fixture-only first orchestration mode, the future orchestrator module name `backend/story_knowledge/extraction_orchestrator.py`, the public API set, strict safe policy flags, in-memory candidate drafts only, no persistence in PHASE8-IMPL-010, owner-review-first handoff, and fail-closed/quarantine behavior. `PHASE8-IMPL-010-T003` added expected-red contract tests in `tests/test_writer_assistant_core_extraction_orchestrator_contract.py`. `PHASE8-IMPL-010-T004` implemented the minimal review-safe orchestration helper in `backend/story_knowledge/extraction_orchestrator.py` with `validate_extraction_pipeline_request`, `build_fixture_extraction_pipeline_plan`, and `run_fixture_extraction_pipeline`; the orchestrator contract passes with 67 tests. `PHASE8-IMPL-010-T005` accepted the candidate review handoff and persistence boundary decision at `docs/roadmap/decisions/PHASE8-IMPL-010-candidate-review-handoff-persistence-boundary-decision.md`: candidate drafts remain in-memory review support only; candidate persistence, review UI/API, owner decisions, apply-promotion, raw artifact persistence, and real runtime extraction remain deferred; evidence/provenance/source locator requirements are mandatory for any future persisted candidate. `PHASE8-IMPL-010-T006` completed a validation-only safety regression pass with orchestrator, parser, raw storage, BookNLP adapter, source/evidence, candidate, and focused OMI/project regressions passing and no runtime hardening patch needed. `PHASE8-IMPL-010-T007` completed roadmap/status closeout. No active child remains under `PHASE8-IMPL-010`. No real BookNLP/spaCy install, import, run, or runtime extraction exists yet. No raw artifact write/read/list helpers, backend extraction routes, frontend extraction/review UI, package changes, automatic candidate JSON persistence from orchestrator output, generated prose, apply-promotion, or memory/canon mutation exists yet.

`PHASE7-IMPL-004` must remain `Chapter / Scene Metadata Compatibility Layer`.

`PHASE7-IMPL-004` is complete as of the T007 closeout. Completed child records:

- `PHASE7-IMPL-004-T002`: read-only backend scene metadata compatibility for legacy Markdown scenes.
- `PHASE7-IMPL-004-T003`: backend scene/chapter metadata write/create helpers.
- `PHASE7-IMPL-004-T004`: route compatibility tests preserving legacy scene route contracts.
- `PHASE7-IMPL-004-T005`: frontend display compatibility for metadata-shaped scene records while preserving legacy string scene IDs.
- `PHASE7-IMPL-004-T006`: legacy scene fallback regression tests.
- `PHASE7-IMPL-004-T007`: roadmap/status closeout.

Final `PHASE7-IMPL-004` behavior preserves existing `scenes/{scene_id}.md` scene bodies, keeps chapter and scene metadata separate from owner-authored Markdown, preserves legacy list/read route shapes, and does not add model calls, generated prose, extraction, OMI/memory/canon mutation, training/JSONL/dataset changes, or browser/manual validation scope.

`PHASE7-IMPL-005` is complete as of the T007 closeout. Completed child records:

- `PHASE7-IMPL-005-T001`: inventory/task/enrichment setup for notes/materials storage and routes.
- `PHASE7-IMPL-005-T002`: backend note/material body and metadata storage helpers.
- `PHASE7-IMPL-005-T003`: backend note/material routes.
- `PHASE7-IMPL-005-T004`: route compatibility and path-safety tests.
- `PHASE7-IMPL-005-T005`: frontend API compatibility helpers.
- `PHASE7-IMPL-005-T006`: minimal notes/materials navigation/display shell.
- `PHASE7-IMPL-005-T007`: roadmap/status closeout.

Final `PHASE7-IMPL-005` behavior stores owner-authored notes and owner-provided materials as separate body and metadata files; keeps body saves from creating metadata; requires existing bodies for metadata writes in this slice; keeps reads/lists side-effect free; enforces safe identity/path derivation; wires route/API/frontend shell compatibility; and does not add generated prose, note/material summaries, extraction, semantic search, model/Ollama calls, metadata editing UI, full shared editor refactor, OMI/memory/canon mutation, or training/JSONL/dataset changes.

`PHASE7-IMPL-006` is complete as of the T007 closeout. Completed child records:

- `PHASE7-IMPL-006-T001`: shared owner-authored document editor inventory and child-task plan.
- `PHASE7-IMPL-006-T002`: shared document state contract and source-level tests.
- `PHASE7-IMPL-006-T003`: shared editor controller helpers.
- `PHASE7-IMPL-006-T004`: document-neutral `Editor.jsx` prop cleanup and behavior parity.
- `PHASE7-IMPL-006-T005`: `ProjectNav.jsx` scene/note/material document selection parity.
- `PHASE7-IMPL-006-T006`: shared editor regression coverage.
- `PHASE7-IMPL-006-T007`: roadmap/status closeout.

Final `PHASE7-IMPL-006` behavior keeps scene/note/material document selection ID-based; loads and saves exact owner-authored body content only; keeps metadata out of editor bodies; preserves dirty-state, keyboard-save, project-switch, and document-switch guards; and does not add metadata editing UI, note/material create/import/upload UI, generated prose, summaries, extraction, model/Ollama calls, OMI/memory/canon mutation, or training/JSONL/dataset changes. Browser/manual validation remains deferred to `PHASE7-IMPL-010`.

`PHASE7-IMPL-007` is complete as of the T007 closeout. Completed child records:

- `PHASE7-IMPL-007-T001`: Project Overview shell inventory and child-task plan.
- `PHASE7-IMPL-007-T002`: overview data contract and source-level tests.
- `PHASE7-IMPL-007-T003`: backend/project data helper compatibility decision; no backend overview helper was needed.
- `PHASE7-IMPL-007-T004`: standalone prop-driven `ProjectOverview` component.
- `PHASE7-IMPL-007-T005`: ProjectNav/App overview integration.
- `PHASE7-IMPL-007-T006`: overview regression coverage.
- `PHASE7-IMPL-007-T007`: roadmap/status closeout.

Final `PHASE7-IMPL-007` behavior adds Overview as a workspace view, not an owner-authored document type. It uses existing deterministic project/list/status state, passes project/scenes/notes/materials/OMI/status/approved-memory shell props, derives scene/note/material counts from arrays/lists only, omits/defer chapter count, resets project switches to Overview, switches scene/note/material selection to editor view, keeps keyboard save editor-view-only, and adds no `/overview` backend/API dependency or backend overview helper. It does not add generated summaries, extraction, semantic search, Story Check auto-runs, model/Ollama calls, OMI/memory/canon mutation, metadata editing UI, note/material create/import/upload UI, training/JSONL/dataset changes, or browser/manual validation.

`PHASE7-IMPL-008` is complete as of the T007 closeout. Completed child records:

- `PHASE7-IMPL-008-T001`: OMI-guided project creation staged flow inventory and child-task plan.
- `PHASE7-IMPL-008-T002`: staged flow data contract and source-level tests.
- `PHASE7-IMPL-008-T003`: backend staged setup storage helpers; Path A selected and backend staged storage deferred.
- `PHASE7-IMPL-008-T004`: backend staged setup routes and compatibility tests; backend staged routes deferred.
- `PHASE7-IMPL-008-T005`: frontend API helpers and staged creation UI shell.
- `PHASE7-IMPL-008-T006`: staged flow regression coverage.
- `PHASE7-IMPL-008-T007`: roadmap/status closeout.

Final `PHASE7-IMPL-008` behavior adds a frontend-transient OMI-guided staged project creation shell with owner-authored setup input, visible setup/candidate labels, review/cancel/reset behavior, and explicit final confirmation before the existing create-project path runs. Backend staged storage and backend staged routes are intentionally deferred. No staged backend API helpers, OMI writes during staged draft steps, hidden pre-confirmation project writes, generated prose, summaries, extraction, semantic search, Story Check auto-runs, model/Ollama calls, apply-promotion, memory/canon mutation, training/JSONL/dataset changes, metadata editing UI, note/material create/import/upload UI, or browser/manual validation were added.

`PHASE7-IMPL-009` is complete as of the T007 closeout. Completed child records:

- `PHASE7-IMPL-009-T001`: Memory / Canon shell inventory and child-task plan.
- `PHASE7-IMPL-009-T002`: approved-memory shell data contract and source-level tests.
- `PHASE7-IMPL-009-T003`: standalone `MemoryCanonShell.jsx` component.
- `PHASE7-IMPL-009-T004`: App/ProjectNav integration as separate `memory-canon` workspace view.
- `PHASE7-IMPL-009-T005`: per-category approved-only empty-state hardening.
- `PHASE7-IMPL-009-T006`: Memory / Canon shell regression coverage.
- `PHASE7-IMPL-009-T007`: roadmap/status closeout.

Final `PHASE7-IMPL-009` behavior adds a read-only Memory / Canon workspace shell with approved-only empty states for characters, locations/settings, timeline, plot threads, continuity/consistency, open questions, relationships, organizations/groups, and objects/items. App/Nav integration exposes the shell as a separate `memory-canon` workspace view. OMI candidates, approved-but-not-applied candidates, and promotion records are not displayed as approved canon. No backend approved-memory routes, backend approved-memory helpers, frontend approved-memory API helpers, apply-promotion, memory/canon mutation, generated prose, summaries, extraction, semantic search, Story Check auto-runs, model/Ollama calls, training/JSONL/dataset changes, metadata editing UI, note/material create/import/upload UI, or browser/manual validation were added. Browser/manual validation remains deferred to `PHASE7-IMPL-010`.

`PHASE7-IMPL-010` is complete as of the T007 closeout. Completed child records:

- `PHASE7-IMPL-010-T001`: docs/status only; inventory, task record, enrichment JSON, and roadmap/status updates.
- `PHASE7-IMPL-010-T002`: automated regression validation pass; all relevant pytest suites passed on first run.
- `PHASE7-IMPL-010-T003`: browser smoke checklist preparation.
- `PHASE7-IMPL-010-T004`: browser/manual smoke execution (PARTIAL; Playwright `libnspr4.so` missing; browser MCP unavailable).
- `PHASE7-IMPL-010-T005`: validation repair triage; no product defect confirmed; no runtime repair authorized.
- `PHASE7-IMPL-010-T006`: final validation regression pass; all suites passed on first run.
- `PHASE7-IMPL-010-T007`: roadmap/status closeout.

Final `PHASE7-IMPL-010` behavior validated the completed Phase 7 workspace foundation from `PHASE7-IMPL-004` through `PHASE7-IMPL-009` using automated regression checks, a prepared browser/manual smoke checklist, partial browser/manual smoke execution with environment-limited fallback evidence, validation repair triage, and final regression confirmation. It did not add generated prose, summaries, extraction, semantic search, Story Check auto-runs, model/Ollama calls, apply-promotion, memory/canon mutation, OMI candidate promotion, backend approved-memory routes/helpers, frontend approved-memory API helpers, metadata editing UI, note/material create/import/upload UI, runtime features, package/dependency changes, or training/JSONL/dataset work. Interactive UI browser validation remains deferred to owner/environment rerun when browser tooling is available.

The published Phase 7 parent sequence (`PHASE7-IMPL-001` through `PHASE7-IMPL-010`) is complete. `PHASE8-IMPL-001`, `PHASE8-IMPL-002`, `PHASE8-IMPL-003`, `PHASE8-IMPL-004`, `PHASE8-IMPL-005`, and `PHASE8-IMPL-006` are complete as the first six published Writer Assistant Core parents in `docs/roadmap/roadmap_index.yaml`. `PHASE8-IMPL-007` is complete as the seventh Writer Assistant Core parent; `PHASE8-IMPL-007-T001` through `PHASE8-IMPL-007-T007` are complete. `PHASE8-IMPL-008` is complete as the eighth Writer Assistant Core parent; `PHASE8-IMPL-008-T001` through `PHASE8-IMPL-008-T007` are complete. `PHASE8-IMPL-009` is complete as the ninth Writer Assistant Core parent; `PHASE8-IMPL-009-T001` through `PHASE8-IMPL-009-T007` are complete. `PHASE8-IMPL-010` is complete as the tenth Writer Assistant Core parent; `PHASE8-IMPL-010-T001` through `PHASE8-IMPL-010-T007` are complete. `PHASE8-IMPL-011 - Writer Assistant Core candidate review queue and persistence gate planning` is now active after `PHASE8-IMPL-011-T006`; T001, T002, T003, T004, T005, and T006 are complete, and `PHASE8-IMPL-011-T007` is ready/active. T002 accepted the persistence gate decision: `candidate_drafts` are validated input support only and future persistence requires an explicit owner-approved gate and existing candidate storage helpers. T003 accepted the review queue data shape and lifecycle decision: queue entries are workflow support only, queue presence is non-approval, lifecycle states are review workflow states only, evidence/provenance/source locator display is mandatory, and grouping/sorting/filtering are workflow convenience only. T004 added expected-red contract coverage at `tests/test_writer_assistant_core_candidate_review_gate_contract.py` encoding the T002 persistence gate and T003 queue boundaries for the future `backend.story_knowledge.candidate_review_gate` module. T005 created `backend/story_knowledge/candidate_review_gate.py` as a pure, standard-library-only, deterministic, candidate-only/review-pending persistence gate over existing candidate validation/persistence helpers, implementing `validate_candidate_draft_for_persistence`, `build_candidate_record_from_draft`, `build_review_queue_entry`, and `persist_candidate_record_for_review`; the target contract now passes (154 tests). T006 validated the candidate review gate safety boundaries as a validation-only pass with the target gate contract (154 tests), candidate regressions (307 tests), orchestrator/source-evidence contracts (171 tests), parser/storage/adapter regressions (397 tests), and focused OMI/project regressions (109 tests) all passing; no safety gap was found, so no runtime hardening patch was needed and `candidate_review_gate.py` was left unchanged. No automatic orchestrator persistence, owner-decision prefill, apply-promotion, memory/canon mutation, review queue storage/listing, review UI/API, routes, frontend UI, raw artifact persistence, runtime extraction, real BookNLP/spaCy runtime, package changes, generated prose, or training/JSONL/dataset work is authorized or was added. No real BookNLP/spaCy install or runtime extraction exists yet.

`PHASE7-IMPL-010` must remain validation-only. It must not add runtime feature scope, apply-promotion, memory/canon mutation, generated prose, model calls, extraction, or training/JSONL/dataset work unless explicitly authorized by a separate roadmap task.

Any prior smoke-test/manual-validation use of `PHASE7-IMPL-004` is a numbering drift issue, not a reason to renumber. Correct the reference by moving validation language under `PHASE7-IMPL-010` or a child task of `PHASE7-IMPL-010`; do not change the identity of `PHASE7-IMPL-004`.

## Authority

- GitHub Issues and GitHub Projects are not authoritative yet.
- Published parent task IDs in `docs/roadmap/roadmap_index.yaml` are authoritative for Codex task prompts.
- Parent task identity is immutable after publication.
- New implementation detail belongs in child micro-tasks, not in renamed or repurposed parent IDs.
- Child micro-tasks may use `planning_microtask`, `runtime_microtask`, or `validation_microtask` types.

## Codex / OpenCode Go Execution Role

Codex and OpenCode Go are strict micro-task implementers, not planners.

Prompts should specify one task ID, the exact files allowed to change, the exact validation commands to run, and the final response format. The agent should not infer new roadmap structure during implementation work.

OpenCode Go is the selected platform for remaining MVP implementation (T014–T026). Model routing follows `docs/roadmap/decisions/PHASE8-IMPL-023-opencode-go-model-routing-and-small-task-execution.md`: `opencode-go/deepseek-v4-flash` for docs/status/small scoped tasks, escalating to `opencode-go/minimax-m3`, `opencode-go/kimi-k2.7-code`, or `opencode-go/deepseek-v4-pro` for runtime/debugging/final-review work. OpenAI, Anthropic, and Google-hosted models are excluded from this implementation workflow.

## Context Tool Boundary

Repomix, Graphify, LeanCTX, and CCE are planning/context tools only and must not be run during Codex implementation micro-tasks.

LeanCTX is exceptional fallback only. It is not part of normal Codex implementation execution for this repository.

Context packs and generated maps are refreshable artifacts, not roadmap truth. The control layer is:

1. `docs/roadmap/roadmap_index.yaml`
2. `docs/roadmap/implementation_status.md`
3. `docs/roadmap/validation/latest_roadmap_validation.md`

## Roadmap enrichment scaffold

- The orchestrator scaffold is local and deterministic.
- The orchestrator must not decide task order.
- The orchestrator must not change `active_frontier` automatically.
- The orchestrator must not mark tasks complete.
- The orchestrator must not modify application code.
- The orchestrator may create task manifests, evidence files, enrichment JSON, and rendered task records.
- Generated context files are evidence artifacts, not source of truth.
- Source of truth remains `implementation_status.md`, `roadmap_index.yaml`, reviewed task records, `decision_log.md`, `risk_register.md`, and `open_questions.md`.
- CCE, Graphify, and Repomix may only be run in explicit collect mode after this scaffold is validated.
- `PHASE7-IMPL-004` should be the first orchestrator test case.

## Local tool command syntax

- Local tool command syntax discovery exists for CCE, Graphify, Repomix, and `scripts/generate_ai_context.sh`.
- Exact discovered syntax is recorded in `scripts/roadmap_enrichment/tool_commands.md`.
- AI Context command candidates for `PHASE7-IMPL-004` are recorded under `.codex-context/PHASE7-IMPL-004/`.
- Command discovery does not mean context collection has run.
- `collect-plan` mode exists for the enrichment orchestrator.
- `collect-plan` creates planned evidence files but does not run context tools.
- `PHASE7-IMPL-004` is the first planned collection target.
- CCE readiness was checked before collection.
- Exact CCE syntax is recorded in `scripts/roadmap_enrichment/tool_commands.md`.
- CCE readiness is now ready for explicit authorized collection.
- This does not mean CCE evidence has been collected.
- The first actual use of CCE should happen only in a future explicit collect step for `PHASE7-IMPL-004`.
- `cce init` remains prohibited.
- `PHASE7-IMPL-004` collection attempt 1 produced mixed evidence.
- Enrichment JSON and task record rendering are intentionally deferred.
- `.codex-context/PHASE7-IMPL-004/collection_repair_plan.md` controls the next evidence pass.

## PHASE8-UX-001 - Master Plan UX / Navigation Proposal

Status: Planned.

Purpose: docs-only UX planning parent to create a master UX/navigation proposal for the current and planned application experience.

Expected outputs:

- `docs/roadmap/ux/PHASE8-UX-001-master-plan-ux-navigation-proposal.md`
- `docs/roadmap/ux/PHASE8-UX-001-impeccable-ui-ux-qa-workflow.md`

Context pack:

- `.codex-context/PHASE8-UX-001/`

Boundary:

- Analysis-only.
- Candidate-first.
- Evidence/provenance-backed.
- Owner-controlled.
- No generated prose or prose-production paths.
- No frontend/backend implementation in this parent unless separately authorized.
## PHASE8-UX-002-T001 Parent Publication

`PHASE8-UX-002 - MVP acceptance UI completion and route wiring` is published as the active MVP-first UI parent. `PHASE8-UX-002-T001` is complete/PASS as docs/status/planning parent publication only.

This parent exists because prior owner acceptance evidence reached `SCRIPT_EXIT=0` but final automated decision remained `MANUAL_REVIEW_REQUIRED` due to missing browser-visible UI/workflow surfaces. The subsequent owner gate is now ACCEPTED by explicit owner decision, without changing the prior automated decision into automated PASS.

Active MVP child sequence: `PHASE8-UX-002-T001` parent publication; `PHASE8-UX-002-T002` UI acceptance matrix + route/workflow decision; `PHASE8-UX-002-T003` expected-red tests for source/Story Check/no-prose UI; `PHASE8-UX-002-T004` owner-authored source/scene create/import/select UI; `PHASE8-UX-002-T005` Story Check diagnostic-only/no-prose evidence UI; `PHASE8-UX-002-T006` Notes/Materials + runtime/review evidence UI; `PHASE8-UX-002-T007` closeout + owner acceptance harness rerun. `PHASE8-UX-002-T002A` is post-MVP/deferred only and is not part of the active MVP child sequence.

External SaaS investigation, Dramatica/current-platform investigation, controlled external experiments, Browsertrix, Crawlee, Stagehand, Playwright against external SaaS, and authorized non-black-box external reference collection are post-MVP/deferred unless the owner opens a separate post-MVP research task. Raw captures remain forbidden from Git. Generated prose/prose-production remains permanently forbidden.

T001 added no frontend/backend/tests/package changes, no crawler scripts, no raw captures, no `.external_sources`, no context bundle, no model/Ollama calls, no generated prose, no canon/memory mutation, no candidate creation, and no apply-promotion shortcut.
- T016D result: `PHASE8-IMPL-023-T016D - Manual real Story Check validation` is complete/PASS. Decision record: `docs/roadmap/decisions/PHASE8-IMPL-023-T016D-manual-real-story-check-validation.md`. T016D confirms the T016C live `story_check` adapter bridges the OMI orchestrator to the existing in-repo `backend.analysis_engine.run_story_check(project_name, scene_id)` callable surface and that the T016C1 strict no-sanitization boundary is exercised end-to-end against a real `qwen3:8b` model response. Project `example`, scene `scene_001` ("The Princess and the Pea"), Ollama `0.31.2` reachable at `http://172.25.144.1:11434`, `qwen3:8b` confirmed via `/api/tags`. Runtime flags: `OMI_LIVE_TOOLS_ENABLED=1 OMI_LIVE_STORY_CHECK_ENABLED=1 OMI_LIVE_STORY_CHECK_SCENE_ID=scene_001 OMI_LIVE_OLLAMA_BASE_URL=http://172.25.144.1:11434 OMI_LIVE_OLLAMA_MODEL=qwen3:8b OLLAMA_BASE_URL=http://172.25.144.1:11434 OLLAMA_MODEL=qwen3:8b OLLAMA_TIMEOUT_SECONDS=600 ANALYSIS_MODE=ollama_baseline`. First manual run with the default 300s (and T015F 180s) timeout confirmed PASS-SAFE-FAIL-CLOSED: live path exercised, `analysis_engine.run_story_check` reached, `Read timed out (read timeout=180.0)` returned, converter failed closed with no findings, no persistence, no Memory/Canon mutation, no promotion records, no apply-promotion, no story prose. Second manual run with `OLLAMA_TIMEOUT_SECONDS=600` (and a direct `run_story_check("example", "scene_001")` smoke test at 324.6s) reached the live model, returned a valid rich-Story-Check dict (5 warnings, 5 suggestions, 5 insufficient_evidence, 4 throughline_alignment, theme_drift, character_consistency), and the T016C1 converter produced 14 T008-validated candidate findings: 4 `Story Check warning` (structural_diagnostic), 4 `Story Check insufficient evidence` (evidence_note), 4 `Story Check throughline diagnostic` (throughline_context), 1 `Story Check storyform diagnostic` (storyform_context), 1 `Story Check character consistency diagnostic` (continuity_warning). All 14 carry `source_adapter: "story_check"`, `provenance.support: "Story Check diagnostic support only"`, `support_label: "Story Check diagnostic support only"`, `owner_decision: {approved: false, decision: "pending"}`, `review_status: "candidate_review_pending"`, `source_locator: "project:example::scene:scene_001"`. The T016C1 strict safety checker entirely filtered the model's `suggestions` field and partially filtered `warnings` and `insufficient_evidence`; the converter did NOT rewrite or sanitize AI/tool/model output. `persist_candidates=False` honored (`persistence_status: "not_requested"`, `new_candidate_ids: []`, `persisted_candidate_ids: []`, `reused_candidate_ids: []`); no candidate files written to `projects/example/omi/candidates/`. All 11 safety envelope flags true. The live adapter did NOT call the legacy `POST /api/projects/{project_name}/story-check/{scene_id}` route. All 114 automated tests pass across the four contract test files. `python3 -m py_compile backend/omi_analysis_orchestrator.py backend/omi_runtime_preflight.py backend/main.py backend/analysis_engine.py` -> exit `0`. `find projects/example -newer /tmp/t016d/result1.json` returned no results (no project files mutated). No backend/frontend/test/package/dependency file was edited. No git add/commit/push was performed. T017 (live BookNLP integration in OMI and analysis) is the recommended next task; the BookNLP runtime is not installed on the owner's hardware, so T017 will start with the runtime availability check.
- T016C1 result: `PHASE8-IMPL-023-T016C1 - Tighten Story Check live converter sanitizer boundary` is complete/PASS. Decision record: `docs/roadmap/decisions/PHASE8-IMPL-023-T016C1-tighten-story-check-live-converter-sanitizer-boundary.md`. T016C1 repairs the T016C converter boundary before T016D manual real Story Check validation. T016C1 found that T016C's `_story_check_sanitize_truth_final_labels` helper with a `_STORY_CHECK_TRUTH_LABEL_REPLACEMENTS` table was rewriting unsafe AI/tool/model output text (approved -> owner-backed, truth -> diagnostic support, canon -> diagnostic context, final -> candidate, promoted -> candidate-only, apply-promotion -> owner-confirmed apply, etc.) so the converted finding could pass T008 validation; this is not an acceptable safety boundary. T016C1 removes the broad text-rewriting sanitizer entirely and replaces it with a strict safety checker + skip-on-unsafe policy. Added `_story_check_legacy_text_is_safe(value)` helper that returns True only when the value is a non-empty plain string without truth/canon/final/approved/promoted/apply-promotion/rewrite/continue/outline/draft/generation language or prose-shaped free text (it reuses the same T008 forbidden patterns the T008 validator uses). Updated `_story_check_safe_excerpt_text` to a plain trim+truncate helper (no sanitization). Updated `_story_check_extract_excerpts_from_value` to filter out unsafe excerpts via `_story_check_legacy_text_is_safe`. Updated `_story_check_make_finding` to add a defense-in-depth safety re-check on the `diagnostic_claim` and the evidence excerpts. Updated `_story_check_warning_label` to return ONLY generic converter-owned labels (Story Check warning/concern/question/insufficient evidence/throughline diagnostic/storyform diagnostic/character consistency diagnostic/diagnostic) without legacy text. Updated `_story_check_result_to_envelope` to skip unsafe items at the item level and to NOT synthesize placeholder text from present/status/reason flags alone. The T008 `validate_story_check_fixture_envelope` validator is unchanged and remains authoritative. 15 new mocked tests in `tests/test_omi_story_check_adapter_contract.py` (canon/approved in warning, rewrite in suggestion, final in concern, promoted in insufficient_evidence, apply-promotion text, unique-phrase-not-sanitized, safe-text-still-converts, generic-labels, partial-safe-and-unsafe-mixed, unsafe-throughline-evidence-skipped, unsafe-theme_drift-reason-skipped, persistence-boundary-safe-after-unsafe-skip, T008-validator-remains-authoritative, existing-fixture-tests-still-pass, existing-T016C-runtime-success-test-still-uses-safe-text). The T016C runtime-success test fixture was updated to use T016C1-safe text (replacing owner-approved/approved wording with owner-supplied/on hand/available wording). `tests/test_omi_story_check_adapter_contract.py` -> `40 passed` (25 existing T008/T016C + 15 new T016C1). `tests/test_omi_tool_assisted_orchestrator_contract.py` -> `30 passed`. `tests/test_omi_tool_assisted_persistence_contract.py` -> `5 passed`. `tests/test_omi_live_runtime_preflight_contract.py` -> `39 passed`. Combined T016C1 validation: `114 passed` across the four contract test files. `python3 -m py_compile backend/omi_analysis_orchestrator.py backend/omi_runtime_preflight.py backend/main.py backend/analysis_engine.py` -> exit `0`. T016C1 does not call real Story Check, real Ollama, the legacy route, or any network; does not mutate Memory/Canon, create promotion records, run apply-promotion, persist candidates, or generate story prose. T016D (manual real Story Check validation against a live qwen3:8b instance behind the new env flags) is the next manual real validation task after T016C/T016C1 are committed.
- T017C result: `PHASE8-IMPL-023-T017C - Manual real BookNLP validation` is complete/PASS-SAFE-FAIL-CLOSED. Decision record: `docs/roadmap/decisions/PHASE8-IMPL-023-T017C-manual-real-booknlp-validation.md`. T017C ran the T017B live `booknlp` adapter end-to-end through `analyze_omi_raw_idea_with_tools` against the installed `.venv-unsloth-clean` BookNLP runtime (`booknlp==1.0.8`, `transformers==5.5.0`, `torch==2.10.0+cu129`, `spacy==3.8.14`, `tensorflow==2.21.0`, `setuptools==80.9.0`) on owner-authored project fixture `projects/example/scenes/scene_001.md` (1971 chars, "The Princess and the Pea" public-domain scene). Runtime flags: `OMI_LIVE_TOOLS_ENABLED=1 OMI_LIVE_BOOKNLP_ENABLED=1 OMI_LIVE_BOOKNLP_MODEL=small OMI_LIVE_BOOKNLP_PIPELINE=entity,quote,supersense,event`; `OMI_LIVE_BOOKNLP_BLOCKED` was not set; `persist_candidates=False`. BookNLP was actually reached: `booknlp.booknlp.BookNLP` was imported lazily inside `_build_booknlp_live_runner`, the BookNLP entity Tagger / coref / speaker model files were downloaded from the Hugging Face Hub on first run, the entity Tagger was instantiated, and `Tagger.load_state_dict(...)` raised `RuntimeError: Error(s) in loading state_dict for Tagger: Unexpected key(s) in state_dict: "bert.embeddings.position_ids"` — a `transformers==5.5.0` vs `booknlp==1.0.8` model state-dict compatibility error (newer `transformers` adds a `bert.embeddings.position_ids` buffer that the older BookNLP model checkpoint does not have). The live runner caught the `RuntimeError` and returned the T017B fail-closed envelope: `state: "failed_closed"`, `candidates_count: 0`, `analysis_status: "fail_closed"`, all 11 safety envelope flags except `no_real_tool_calls` (which the live path flips to `false` as designed) returned `true` (`no_prose`, `no_memory_canon_mutation`, `no_apply_promotion`, `no_canon_promotion`, `no_package_installs`, `no_story_prose_generation`, `candidate_presence_is_not_canon`, `queue_presence_is_not_approval`, `support_is_not_truth`, `tool_output_is_not_canon`). `persist_candidates=False` was honored (`persistence_status: "not_requested"`, `persisted_candidate_ids: []`, `new_candidate_ids: []`, `reused_candidate_ids: []`); no candidate files were written to `projects/example/omi/candidates/`, no `projects/example/omi/index.json` mutation, no `projects/example/omi/promotions/` file change. No Memory/Canon mutation, no automatic promotion records, no automatic apply-promotion, no story prose. Combined automated validation: `tests/test_omi_booknlp_spacy_adapter_contract.py` -> 30 passed, 1 failed (pre-existing `test_live_spacy_missing_package_returns_unavailable` unrelated to T017C; spaCy is now installed locally in `.venv-unsloth-clean`); `tests/test_omi_tool_assisted_orchestrator_contract.py` -> 30 passed; `tests/test_omi_tool_assisted_persistence_contract.py` -> 5 passed; `tests/test_omi_live_runtime_preflight_contract.py` -> 50 passed. `python3 -m py_compile backend/omi_analysis_orchestrator.py backend/omi_runtime_preflight.py backend/main.py backend/analysis_engine.py` -> exit `0`. `python3 -m json.tool docs/roadmap/enrichment/PHASE8-IMPL-023.enrichment.json` -> VALID. T017A/T017B runtime caveats (CPU fallback, `setuptools==80.9.0`/`pkg_resources` pin, `torch==2.10.0+cu129` C++ extension warning, first-run model download) remain in effect; the proximate cause of the T017C PASS-SAFE-FAIL-CLOSED outcome is the new `transformers` `bert.embeddings.position_ids` state-dict key. No backend/frontend/test/package/dependency file was edited in T017C; no git add/commit/push was performed. `PHASE8-IMPL-023-T017C1` is the recommended next task as a narrowly scoped repair task to address the `transformers`/BookNLP model state-dict compatibility and rerun T017C. If T017C1 reaches a real candidate-only findings stream, T017 closes to PASS and the next task is `PHASE8-IMPL-023-T018A — NCP schema validator preflight`.
